# Historical upstream selector from the retired relay-host cutover.
# Keep this as a variable so nginx resolves the selected Docker network alias
# at request time rather than pinning an upstream at nginx startup.
map $host $roomview_upstream {
  default materializer;
}
