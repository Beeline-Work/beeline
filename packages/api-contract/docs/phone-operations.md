# Phone API operation inventory

`PhoneOperationMap` in `src/phone-operations.ts` is the type authority for the phone's server operations. Mobile writes use the monolith API exclusively. Every operation is domain-specific; there is no event-query or event-publish escape hatch.

## Durable reads

RoomView DTOs and guards cover the `workspaces`, `workspace`, `agent`, `chats`, `room`, `corners`, `history`, `invite`, and `claimAgentPairing` reads in `packages/buzz-client/src/room-view.ts`. Live invalidation arrives separately over `/v1/phone/live`; server-indexed RoomView responses remain the read authority.

## Writes, authentication, media, and GitHub

| Named operation                                                                                             | Current mobile call sites covered                                                                                                                             |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sendRoomMessage`, `sendRoomReply`                                                                          | Chat composition and retry in `[channelId].tsx` and `useRoomSurfaceSession.ts`; `MonolithRigTransport.publishPreparedMessage`                                 |
| `createRoomSchedule`, `listRoomSchedules`, `deleteRoomSchedule`                                             | Agent-managed recurring work on the monolith server; mobile managers can inspect or stop existing work in `sources/app/(app)/beeline/settings/schedules.tsx`. |
| `decideWritePermission`                                                                                     | `MonolithRigTransport.respondToWritePermission`; permission decisions/executions/revocations in `sources/buzz/factory-permissions.ts`                         |
| `createWorkspace`                                                                                           | Workspace creation in `sources/app/(app)/beeline/community.tsx` and `sources/buzz/workspace-bootstrap.ts`                                                     |
| `addWorkspaceMember`                                                                                        | Role management in `sources/app/(app)/beeline/MembersScreen.tsx`; `MonolithClientAdapter.addMember`                                                           |
| `updateWorkspace`                                                                                           | Workspace name, avatar, and visibility in `sources/app/(app)/beeline/settings/workspace.tsx`; `MonolithClientAdapter` workspace methods                       |
| `leaveWorkspace`                                                                                            | `MonolithRigTransport.leaveWorkspace`                                                                                                                         |
| `createRoom`                                                                                                | Room creation in `sources/app/(app)/beeline/channels.tsx` and `sources/buzz/factory-permissions.ts`; `MonolithRigTransport.createRoom`                        |
| `updateRoom`                                                                                                | Room name and visibility actions in `[channelId].tsx` and `settings/workspace.tsx`; `MonolithClientAdapter` room methods                                      |
| `deleteRoom`, `leaveRoom`                                                                                   | Room actions in `[channelId].tsx`; `MonolithRigTransport.deleteRoom` and `leaveRoom`                                                                          |
| `addRoomMember`, `removeRoomMember`                                                                         | Room member actions in `[channelId].tsx` and `sources/buzz/factory-permissions.ts`; `MonolithRigTransport` member methods                                     |
| `resolveDirectMessage`                                                                                      | `MonolithRigTransport.resolveDirectMessage`                                                                                                                   |
| `createInvite`, `resolveInvite`, `redeemInvite`                                                             | `MonolithClientAdapter.createInvite`; `CommunityInviteJoin.handleJoin`; `RoomViewClient.invite`                                                               |
| `createAgentPairingCode`, `claimAgentPairing`                                                               | Pairing in `sources/app/(app)/beeline/MembersScreen.tsx`; `RoomViewClient.claimAgentPairing`                                                                  |
| `updateAgentSoul`, `updateAgentModelSelection`, `updateAgentYolo`, `updateAgentAccessPolicy`, `removeAgent` | Agent-owner configuration and Workspace-manager removal in `sources/app/(app)/beeline/MembersScreen.tsx`                                                      |
| `updatePersonProfile`                                                                                       | Profile actions in `sources/app/(app)/beeline/onboarding.tsx`, `settings/identity.tsx`, and `sources/buzz/person-name.ts`                                     |
| `setRoomRepository`, `setRoomTargetBranch`, `setRoomGitHubEvents`                                           | `MonolithRigTransport.roomRepositorySet`, `roomTargetBranchSet`, and `roomGitHubEventsSet`                                                                    |
| `approveCornerMerge`                                                                                        | Manager-only squash merge for the webhook-owned corner PR; `force` explicitly overrides known failing checks                                                  |
| `getAuthCapabilities`                                                                                       | No direct mobile call site after retirement of the relay transport                                                                                            |
| `beginGitHubIdentityBind`, `completeGitHubIdentityBind`, `recoverGitHubIdentity`                            | GitHub binding and recovery in `sources/app/(app)/beeline/onboarding.tsx` and `settings/identity.tsx`                                                         |
| `getIdentityRecovery`, `getManagedIdentity`, `adoptGitHubHandle`, `claimManagedHandle`                      | Managed-handle onboarding and settings flows in `sources/app/(app)/beeline/onboarding.tsx` and `settings/identity.tsx`                                        |
| `listGitHubRepositories`                                                                                    | `MonolithRigTransport.workspaceGitHubAccess`                                                                                                                  |
| `beginGitHubInstallation`                                                                                   | `MonolithRigTransport.githubInstallationStart`; return-state handling in `sources/auth/github-auth-session.ts`                                                |
| `createGitHubRepository`                                                                                    | `MonolithRigTransport.githubRepositoryCreate`                                                                                                                 |
| `getGitHubRepositoryAccess`                                                                                 | `MonolithRigTransport.githubRepositoryAccess`                                                                                                                 |
| `uploadMedia`                                                                                               | `sources/buzz/avatar-upload.ts:49`; `sources/buzz/chat-attachment.ts:73,101`                                                                                  |
| `registerPushDevice`, `unregisterPushDevice`, `sendPushTest`                                                | `sources/push/buzz-push-registration.ts:255-347,390-414`                                                                                                      |
| `reportRunningUpdate`                                                                                       | `sources/push/update-receipt.ts:58-89`                                                                                                                        |

Local-only SecureStore, AsyncStorage, clipboard, image preparation, browser-return state, optimistic outbox state, and notification permission calls are not server operations and therefore are not included.

The cutover client supplies an optional random 32-byte-hex `messageId` to send/reply so its optimistic row and retry use the same authoritative identity. Invite redemption returns the joined `workspaceId`; mobile sends the redemption through the monolith phone API while `RoomViewClient.invite` resolves the preview.
