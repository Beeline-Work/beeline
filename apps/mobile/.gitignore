# Expo prebuild output — generated per-platform; not committed
/android/
expo-env.d.ts

# release-proof.mjs evidence output (screenshots, dumps, JUnit) — uploaded as a CI artifact, never committed
/release-proof-out/
