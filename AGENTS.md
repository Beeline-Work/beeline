# Project agent memory

Committed home for project-intrinsic agent knowledge: rule + authoritative file + coverage pointer. The named code and tests hold the detail; do not re-expand prose here.

## Core invariants (read first)

- **kind:30078 reads filter by `#d`, NEVER `#h`** (a `#h` filter over kind 30078 matches nothing). `agentPresenceKey(channelId)` (`packages/buzz-client/src/agent-presence.ts`) is the one presence-key builder. Generic relay stubs cannot catch this — use `matchesFilterRelayFaithfully`/`stubRelayFaithfulPublishing` (body.test.ts). Source assertion: `agent-presence.test.ts`.
- **Production relay may answer a multi-value `#h`/`#d` filter with matches for only one key.** Every fan-out in mobile transport and `packages/buzz-client` expands to one single-key filter per key inside ONE batched `query([...])`/subscription; fake relays answer multi-key filters first-key-only (`buzz-rig-transport.test.ts`, `client.test.ts`).
- **Local stack vs production:** `repository-truth.ts` maps `git://` markers to `https://`, unusable against the local docker stack (`relay-stack/compose.yml`, http 127.0.0.1:3010) — pair repo-less and invite top-level Rooms. That stack includes a credential-free materializer, so local RoomView reads stay on 3010. Mobile's `getEffectiveRelayUrl()` honors the embedded runtime URL when no device override exists, and `BuzzClient` treats loopback/Android-host relay URLs as the intentionally open local relay instead of waiting forever for a NIP-42 challenge; coverage: `buzz-identity-storage.test.ts`, `client.test.ts`. Gate helpers resolve the relay from env AT IMPORT TIME: set `BUZZY_RELAY_HOST=127.0.0.1:3010`, `BUZZY_RELAY_SCHEME=http`, `BUZZY_RELAY_URL=http://127.0.0.1:3010`, `BUZZ_RELAY_URL=ws://127.0.0.1:3010` BEFORE importing `@beeline/gate`, or reads/writes silently go to PRODUCTION.
- **Private Workspace pairing does not require a relay-wide membership gate.** `visibility='invite-only'` maps to NIP-29 `private`; an outsider redeems the globally readable kind:30078 marker through the authenticated `/agent-pairing/claim` server boundary, which atomically verifies the marker's current member author and reserves it for that exact key before the client publishes canonical kind:9000 membership. Acceptance proof: `npm run prove:private-pairing` against the ordinary open local relay.
- **Hand-testing `beeline start`/`pair` in a worktree of this repo can start a REAL production daemon** (runtime pointers live under the shared git common dir). Always pass an isolated `XDG_STATE_HOME` (and `--repo` to a throwaway repo for `pair`).
- **Thin corners are GitHub-event driven after one explicit start and have one agent owner.** `DaemonService.createCorner` records `corner_facts.owner_agent_id`; daemon listing and every corner write admit only that agent. Migration/import backfills legacy corners from an agent `created_by` or their first agent-authored message; RoomView uses the same fact for the header. `MonolithCornerTurnLoop` owns the isolated worktree/session, while `github-operations.ts` posts PR/check facts and archives only from the merge webhook. A restored helper must never re-announce `working` over existing PR/check lifecycle; a later PR webhook re-derives a damaged old fact by its durable feature branch. The corner agent may merge with `gh` only after the server-posted checks-passed note and `pr_checks_status`, and must honor a human hold. **One check turn per changed server state, never per note** (`corner-checks.ts`): a poll's completed `GitHub passed/failed a check` notes start ONE turn only when `getCornerRestoreState().lifecycle.checks` is `passing`/`failing` and differs from the last turn's state (`started a check` resets); `pr_checks_status` reads that same server verdict; a checks-turn reply that only restates the notes is dropped (`isCornerStatusRestatement`), and a top-level turn that called `open_corner` drops its own `Opened corner …` echo (`stripCornerOpenEcho`, `reply-sanitizer.ts`) because the server card already says it. Coverage: `apps/server/src/integration.test.ts`.
- **Retired daemon prose has one tombstone authority; a shape carrying variable data (a path, a model name) gets a STRUCTURAL matcher, not an exact-set entry.** `packages/buzz-client/src/retired-agent-notices.ts`'s `isRetiredAgentNotice` (exact-set match, plus bounded regex shapes: the attachment-delivery ENOENT dump, the model-unavailable startup wall), applied at three reader floors — `RoomIndexer.projectEvent`, `room-list-row.ts`'s stored-preview check, and mobile's `retired-agent-notices.ts` cache scrub. Coverage: `retired-agent-notices.test.ts`, `room-list-row.test.ts`, `surface-storage.upgrade.test.ts`.
- **Agent Grants have one vocabulary and one authority axis.** `packages/api-contract/src/agent-grants.ts` owns kinds, statuses, the command-rule parser (`parseCommandGrantTarget`: exact line, no shell metacharacters, `--with SECRET` suffixes; `commandGrantMatches` is a word-for-word argv prefix), and the decision line (`formatGrantDecisionLine`/`parseGrantDecisionLine`, `<decider> approved command npm test`, no colon) that the server posts as a system line mentioning the agent so the daemon wakes (`isGrantDecisionLine`; plain system lines never trigger). Store: `agent_grants`; `DaemonService.requestAgentGrant` (yolo → `auto=true` + one quiet `grant-auto` line, never for `budget`; else one `grant-request` card per agent+Room per 2-minute window, mentioning the owner so the ordinary push fires); `PhoneService.decideAgentGrant`/`revokeAgentGrant` are gated to the agent owner or a Workspace manager (`YOLO_AUTHORITY_MESSAGE`) and settle the card in place. Daemon: `apps/body/src/grant-runner.ts` serves `run_granted_command` over a loopback door (`BEELINE_GRANT_RUNNER_URL/TOKEN` in the beeline-agent MCP env), cwd = Room checkout/corner worktree, env = PATH+HOME+named secrets (operator key store / `secrets.json`, scrubbed from output), a `once` grant is consumed before its run, and every run is one `postAgentActivity` tool row carrying `requestedBy`. Applying path/host/secret/device/budget to the sandbox is slice 3. **A corner strictly dominates a Room, and the build enforces it (C94).** `packages/api-contract/src/surface-capabilities.ts` is ONE table (rows: read-repository, write-scratch, write-worktree, write-checkout, run-host-command, read-secret, reach-network; `surface-capabilities.test.ts` fails on any capability a Room has and a corner lacks). It drives, not documents: `grant-runner.ts` spawns a granted command plainly when `surfaceAllows(surface,'run-host-command')` (corner — host work belongs there) and otherwise into that Room's own `mode:'readonly'` bwrap table, keeping the session scratch and `agent-home.ts` overlay writable while the repository copy and live host go read-only; with no usable bwrap a Room run is REFUSED (fail-closed, unlike the harness spawn path). **Yolo is the scope gate and there is no per-command approval**; exactly two hard stops survive it on both surfaces — a credential/env file named by argv (`namesCredentialFile`, re-checked per run against the approved prefix by `commandGrantEscalationsBeyondRule`) and a script nobody has read. The latter binds the approval to BYTES: the MCP tool reads the file (checkout or scratch only, seeing through `env`/`sudo`/`timeout` wrappers), the card inscribes it in the machine role, over 4096 bytes/120 lines is refused not truncated, and the runner re-hashes before every run. The yolo auto line names the boundary it licensed (`surfaceGrantBoundary`). Coverage: `agent-grants.test.ts`, `surface-capabilities.test.ts`, `apps/server/src/integration.test.ts`, `grant-runner.test.ts` (live bwrap proof), `agent-grant-tools.test.ts`, `monolith-room-turn.grants.test.ts`, mobile `RoomMessageVariants.test.tsx`/`MembersScreen.management.test.tsx`.
- **Removing an agent RETIRES it; the record of its work survives.** `PhoneService.removeAgent` (one transaction) ends memberships and daemon tokens, sets `identities.hidden_from_roster`, clears the `agents` row's configuration in place (the row stays: grant history and the agent read join it for `owner_id`), deletes that Workspace's `agent_schedules`, revokes outstanding `agent_grants` and settles their cards through the one `settleGrantCard` helper, and archives corners the agent owned as `done`/`abandoned` — `agent_turns` and every message are left alone. A helper retires itself only on the one settled answer: a daemon request whose token is revoked AND whose agent holds no live membership is refused 403 `agent_removed` (`TokenAuth.retiredDaemonAgent` → `refuseDaemon`), which `isAgentRemovedError` turns into `ThinDaemonCore`'s `agent-removed` and `retireRemovedAgent` (disable unit, archive runtime into `deleted-runtimes/`); transport failures, 5xx and a plain 401 must never trigger it. Re-pairing the same key upserts identity/agents/membership back to a freshly seeded shape. A grant revoked by removal is refused under C94 too: `GrantCommandRunner.run` re-fetches `listAgentGrants` on every `run_granted_command` call and matches argv against that live list, so a revoked/settled grant simply stops matching before `surfaceAllows` is even consulted — no separate revocation check needed at the surface boundary. Coverage: `agent-removal.test.ts`, `agent-retirement.test.ts`, `apps/server/src/integration.test.ts`.
- **An agent's animal is its face, its name AND its soul — one vocabulary, assigned by the server.** `packages/api-contract/src/faces.ts` owns `FACE_SOULS`/`FACE_NAMES` beside `FACE_IDS` (one entry per id; `faces.test.ts` fails on drift) plus the pure `assignSeededAgentIdentity` (first animal no current Workspace member wears, in a `seededFaceOrder` permutation of the agent's key; all twelve taken → `defaultFaceForSeed`, and the name repeats with a roman numeral so it stays a spoken name). Only the server knows the roster, so `PhoneService.claimAgentConnectPairing` runs it inside the claim transaction under `workspaces … FOR UPDATE` and writes `identities.face_id` + `identities.name` + `agents.soul` together. **The CLI asks for neither a name nor a soul** (`connect-command.ts`: the wizard asks only what it could not find out (see the connect entry below), then `confirmSeededName` prints `Foxy the fox` and offers keep/rename through `POST /auth/agent/connect/name`, authorized by the pairing code for 15 minutes after the claim and degrading to the seeded name on failure); clack's cyan rails are repainted by `paintWizardBrass`/`brassRails` and `brass()` is #D7AF5F (xterm 179). `workspaces.seeded_souls_enabled` (manager-gated via `updateWorkspace`, `WorkspaceView.managerSettings.seededSouls`, the switch on `MembersScreen`) suppresses the persona at BOTH daemon seams — `DaemonService` configuration and roster — leaving only `SOUL_HOUSE_RULE` (`apps/body/src/response-directives.ts`), the one line both turn loops carry beside `Soul instructions:` whether or not a soul exists. `AgentDetailView.seededSoul` is what the app's soul editor restores to. Coverage: `faces.test.ts`, `phone-service.agent-connect.test.ts`, `apps/server/src/integration.test.ts`, `connect-command.test.ts`, `monolith-corner-turn.test.ts`, `monolith-room-turn.attachments.test.ts`, `MembersScreen.management.test.tsx`.
- **The face ceremony has one vocabulary in two pinned copies.** `packages/api-contract/src/faces.ts` (`FACE_IDS`, `isFaceId`; the server refuses anything else in `PhoneService.updateFace`, `identities.face_id`) and `apps/mobile/sources/buzz/faces/index.ts` (same twelve ids + `defaultFaceForSeed` = Speakeasy's FNV-1a-into-12 + `resolveFace`; kept local so no screen depends on a fresh SDK `dist/`). Every identity view exposes `face?` through the one `identity()` projector in `phone-service.ts`; the phone passes it into `IdentityMark`'s `face` prop everywhere an identity is drawn. Onboarding runs `FaceCeremonyStep` only after a monolith sign-in (`enterAfterMonolithSignIn`; a face already on record skips it); Settings changes it through `FacePickerSheet`. Coverage: `integration.test.ts` (face ceremony), `FaceCeremonyStep.test.tsx`, `FacePickerSheet.test.tsx`, `onboarding.github-callback.test.ts`.
- **A mobile fix must not depend on a brand-new `@beeline/buzz-client` export** (Metro resolves built `dist/`; a missing symbol inside `.catch` fails closed silently) — add a `testID` and grep the bundle. `apps/auth` tests also import `dist/`: rebuild `@beeline/nostr` and `@beeline/buzz-client` after SDK edits.
- **Deleted features stay deleted:** daemon health chat (`agent-state-messages.ts`, five sentences; `agent-state-messages.test.ts` is the TOMBSTONE), model-unavailable/model-validation chat diagnostics (`model-availability.ts`; startup remains blocked through machine state and recovers through agent settings), the client "X seems offline" notice (`transcriptSurfaces.test.ts`), photo overrides (`PHOTO_OVERRIDES_ENABLED` ships `false`, `photo-overrides.ts`; `identity.photo-darkflight.test.ts`). A down agent is shown only by presence lease, corner/turn state, daemon log. The Settings Appearance theme picker (`/settings/appearance`, `themePreference`, the Editorial Ink / Ledger token sets, IBM Plex Serif) is retired: `groknight.ts` ships Obsidian Refined only (`groknight.test.ts`, `leaf-surface-contract.design.test.ts`).

## Server-indexed surfaces and Room reads

- **Monolith Workspace/Room membership follows the retained phone contract:** everyone lands in `#welcome` — fixed ids in `@beeline/api-contract/phone` (`default-workspace.ts`); `apps/server/src/default-workspace.ts` (`seedDefaultWorkspace`, run by `migrate`) seeds Workspace + Room once (`about` only on creation), silently backfills every human without resurrecting a leave, and mirrors Workspace owner/admin roles onto the Room; auth joins each sign-in through `joinRooms`; the phone's `buzz/welcome-landing.ts` pushes the Room over the deck once per identity (`channels.tsx`, monolith only; `welcome-landing.test.ts`, `channels.bootstrap.test.ts`, `default-workspace.test.ts`). A new Room starts creator-only; person-invite redemption is reusable/retry-safe and attaches the person to every existing live top-level non-DM Room; later Rooms require explicit adds; DMs are deterministic and limited to current Workspace members. Coverage: `apps/server/src/auth.test.ts`, `apps/server/src/integration.test.ts`.
- **Existing top-level Room joins go through `apps/server/src/membership-join.ts`:** each newly inserted/restored Room membership gets one `presentation='system'` join note, while one workspace-scoped push outbox event snapshots existing humans' registered devices so N Room notes never become N pushes. `background.ts` excludes those notes from ordinary message fan-out. Coverage: `apps/server/src/integration.test.ts`, `apps/server/src/auth.test.ts`.
- **Server-indexed surfaces are the only durable Room/Workspace read authority.** `apps/push-gateway/src/room-indexer.ts` owns paint-ready queries; `packages/buzz-client/src/room-view.ts` names DTOs + signed client; screens render validated responses verbatim. A relay signal only marks a surface dirty; only verified draft/thought/presence events enter `LiveOverlayDecoder`. Never restore a client parser/reducer/selectors/Workspace snapshot. Typed kind:9 events are non-conversation unless explicitly marked; `Body.agentHistory` consumes only `presentation='message'`. Coverage: `surface-runtime.test.ts`, `room-indexer*.test.ts`. A Body test whose fixture only models relay `/query`/publish must not exercise `agentHistory`/`indexedRoomMessages` (a real authenticated `GET /room/:id`) — stub it directly (`stubEmptyAgentHistory` pattern, `body.test.ts`); an upload flow needs `mediaUploadResponse` (`relay-test-helper.ts`), never `JSON.parse`d like an event publish. `apps/body` is CI-gated on PRs (`BODY SUITE` in `.github/workflows/checks.yml`).
- **Turn progress is server-indexed receipt lifecycle** (`RoomView.latestAgentTurns`; mobile lights a fresh `working`, clears on `complete`/`failed`/contradiction/90s horizon). Draft/thought overlays never drive the thinking indicator; the local pre-receipt "sending…" ack (`selectComposerAckState`) is a separate, earlier stage that the real receipt always supersedes. **Workspace pictures are current kind:39000 metadata**, never only the kind:9007 create.
- **Every system notification has ONE producer and ONE grammar.** `apps/server/src/system-line.ts` (`systemLine`/`restateSystemLine`/`composeSystemLine`) phrases every membership join/leave/removal, yolo flip, grant request/decision/auto line, failed turn and its settle, GitHub pull-request/push/check note, corner open/merge card header, permission and target-branch card header, and scheduled prompt as `<subject> <verb>[ <object>][ · <consequence>]` (`@beeline/api-contract` `system-events.ts`: past-tense plain verb, no colon/em dash/trailing period/URL; the URL rides on the object) and stores the structured event in `messages.system_event` beside the text. `system-line.contract.test.ts` fails any `presentation='system'`/`'card'` insert or text restatement outside that file; the daemon never phrases a line (`postRoomMessage` coerces `presentation` to message/card). Phone: `RoomViewMessage.systemEvent` → `ChatDisplayMessage.systemEvent`, `buzz/system-lines.ts` folds same-verb runs ("Candy, Terra and Codex joined"), and `LedgerSystemLine` (`Ledger.tsx`) is the sole renderer — `meta` role, no avatar, stamp right, names brass and tappable, object linked by URL; a row without `system_event` renders its text verbatim. Coverage: `system-line.test.ts`, `system-line.producers.test.ts`, `integration.test.ts`, mobile `system-lines.test.ts`, `SystemLine.design.test.ts`.
- **A failed agent turn is a fact the Room carries.** The daemon sends `reason` (one line, ≤200 chars, no stack, secrets scrubbed: `apps/body/src/turn-failure-reason.ts`, used by both Room and corner loops) with its `failed` receipt; `DaemonService.turnReceipt` stores `agent_turns.failure_reason` and, when the request had a human trigger, inscribes ONE `presentation='system'` line `<agent> could not answer · <reason>` (`card_type='turn-failed'`, mentioning the requester so the push fires once); retries of the same request within 10 minutes update that row, and a later `complete` receipt or durable reply settles it in place to `<agent> answered after a retry`. Coverage: `turn-failure-reason.test.ts`, `monolith-room-turn.failure.test.ts`, `monolith-corner-turn.test.ts`, `apps/server/src/integration.test.ts`.
- `surface-storage.ts` persists only response + outbox partitions; cold open installs invalidation first, then exactly one signed GET. **Read marks are server-owned** (`beeline_room_read_marks`, `apps/push-gateway/src/database.ts`); never add a local read-mark store. A cold Room-list bootstrap must reach a terminal UI state (`channels.tsx`).
- **A NIP-10 reply parent is an opaque same-Room proof** (`messageSubmitReply`; `reply-command.ts` alone owns structural tags). `relay-error.ts` is the sole interpreter of publish refusals (`RelayPublishError`, raw text out of `message`; 408/429 retryable). Coverage: `relay-error.test.ts`, `publish-delivery.test.ts`. **A ledger entry carrying a reply-reference always re-announces its speaker** — run-continuation byline suppression never applies to a reply; ambiguity resolves toward showing the byline (`ledger-attribution.ts`).
- **One presentation authority for floating surfaces:** `ModalManager.ts`; `HullDialog.tsx` owns the sole RN `Modal`; `HullActionSheet.tsx` the sheet. Direct `Alert.alert`/system sheets/native menus prohibited (`native-dialog-enforcement.test.ts`). Its `KeyboardAvoidingView` must come from `react-native-keyboard-controller` (never RN's own — the app hands keyboard tracking to `KeyboardProvider` app-wide in `app/_layout.tsx`, and RN's own view never learns an already-open keyboard's height at mount, snapping late when a sheet opens over a focused composer); Android behavior is `translate-with-padding`, never `height` (`keyboardAvoidance.test.ts`).

## Branch conventions

- **Corner agents own the GitHub workflow.** Finish means commit, push the feature branch, run `gh pr create`, print the PR URL, then wait for the server checks-passed fact. With no human hold, the agent merges with `gh`; the server merge webhook archives and the helper reaps the worktree.
- `fm/*` branches stack; check `gh pr list --json baseRefName,headRefName` before assuming a file is on `main`; `fm-integration-verify` ↔ `origin/fm/buzzy-integrated`; `git branch -a --contains <commit>` finds the source branch.
- Cherry-picks off `feature/*` can omit a declaration referenced only as context — `npm run typecheck` catches it. Two branches adding trailing params to one function: match slots to call sites on `origin/main`. Merging `origin/main` into stale `fm/*` conflicts mostly on `relay-stack/web/dl/` artifacts — pick either, regenerate via `npm run bundle:beeline`. Interleaved `describe(...)` conflicts: rebuild siblings from `git show <sha>:<path>`, verify with `npx esbuild`.
- **Resolve merges as one batch gate:** enumerate every textual conflict and semantic overlap before editing, resolve the complete set in one pass, then run one integration matrix spanning every affected subsystem plus a single conflict-resolution review before push. Do not push or review piecemeal conflict fixes.
- **`TYPECHECK` (`.github/workflows/checks.yml`, `npx turbo run typecheck`) gates every PR unfiltered by path** (it is the one gate there with no path filter): two independently green PRs can merge cleanly into a red `main` (#850 + #851 on `IdentityRow.face_id`), so a required-field change on a shared row type must re-run the root typecheck after merging `origin/main`, not only the package's tests.
- **Stacked PRs merged base-first do not propagate** — re-target to `main`; verify with `git grep <symbol> origin/main`.

## Corner GitHub credential boundary

- **Every thin repository corner receives one exact-repository GitHub App token.** `getRoomGitHubToken` mints through the server Room-token service; `room-runtime.ts` installs a worktree-only git credential helper and `MonolithCornerTurnLoop` injects `GH_TOKEN`/`GITHUB_TOKEN`. Host credential stores remain masked by `bwrap-sandbox.ts`.
- Residual (docblock `bwrap-sandbox.ts`): no mask list enumerates every secret on a shared machine; docker.sock/systemd user bus pierce the sandbox by owner choice; recommended posture is a credential-less user account (not built).

## Gate / live relay tests

- Gate `apps/gate/`; live `cd apps/gate && npm run test:live` after `npm run stack:up`. Relay defaults `relay-config.ts` → `usebeeline.app` (`relay.buzzrouter.com` permanent alias). Room tenancy is server-stamped `channels.community_id` (`AuthStore.relayCommunityIdForRoom`); the kind:9007 `community` tag is NOT the tenant boundary.
- GitHub sign-in: `github-auth-session.ts` keeps browser authorization and ticket exchange on one stack (`monolithUrl` when enabled, legacy `relayUrl` otherwise); identity is sealed pre-browser, `/auth/oidc/bind` is create-once, and `/auth/oidc/recover` is explicit replacement (`github-auth-session.test.ts`, `onboarding.github-callback.test.ts`).
- Provisioning lives in `apps/gate/src/provisioning.ts`; factory-permission human custody/role verification lives in `human-authority.ts`.

## Body (@beeline/body)

- **The helper is a monolith-only thin Room client.** `ThinDaemonCore` → `RoomRuntimeCoordinator` → `MonolithRoomTurnLoop`; discovery, inbox, authority, roster, conversation, receipts, drafts, replies, activity, and presence all use `DaemonApiClient`. A runtime without `transport.kind='monolith'` is refused and must reconnect through the app.
- **The sole thin daemon action surface is repository corner start/status.** Relay-backed `Body`, pairing redemption, approvals, mandates, daemon-local repository lifecycle, relay sockets, and event publishing are deleted. `read-only-mcp.ts` exposes `open_corner` in top-level Rooms and `pr_checks_status` in corners through the separately named `beeline-agent` MCP surface. **A corner's NAME and OBJECTIVE do different jobs; `corner-text.ts` is the one authority for both.** `open_corner` requires each: the name (≤3 words) is `rooms.name` and titles every surface (Room-list child row, header, both cards, the push); the objective (≤24 words) is `corner_facts.objective`, the card body and the corner's opening message. Both are FLATTENED (line breaks, tabs, double spaces) before judging, and only a genuinely over-long text is refused — in a sentence naming the limit and the count. A legacy corner holds its objective in the name slot, so every title funnel shows the first three words (`cornerDisplayName`; mobile keeps its own `cornerName` so no screen needs a fresh `dist/`). **A refused tool call is an MCP `isError` RESULT, never a JSON-RPC protocol error** (grok surfaces a protocol error only as "failed via `use_tool`"), and both turn loops log every failed tool call (`tool-call-failure.ts`). Coverage: `corner-text.test.ts`, `read-only-mcp.open-corner.test.ts` (grok's captured frames), `tool-call-failure.test.ts`, `apps/server/src/integration.test.ts`.
- **Top-level Rooms: read-only filesystem, unrestricted tools.** The filesystem rule is held by bwrap, not by the harness mode: under an active OS sandbox a codex-acp Room selects `agent-full-access` like a corner (Codex's own `read-only` mode is also offline), and only an unwrapped codex Room keeps Codex's offline read-only mode (`roomModeCandidates`, `harness-capabilities.ts`; live proof in `bwrap-sandbox.test.ts`). Each session mounts `beeline-readonly-mcp` plus the host-governed `beeline-agent` corner opener, and approves EVERY mounted MCP tool call (the sandbox is the boundary, not the tool list; Trusty Squire stays host-broker-gated). **A permission decision reads the tool's IDENTITY, never the harness's wrapper shape** (C90): grok routes EVERY MCP call through its native `use_tool` dispatcher — titled `use_tool`, the qualified `<server>__<tool>` only in `rawInput.tool_name`, no `mcp` token anywhere — so it resolves against the servers this session mounted (`resolveMountedMcpToolCall`, threaded from the Room's own `servers` array), fail-closed, envelope unwrapped before `shellPayload`, Squire refused first. Every harness's captured spelling is tabulated in `read-only-policy.ts`; frames in `fixtures/grok-use-tool-permissions.ts` and `fixtures/claude-agent-acp-permissions.ts`, coverage `monolith-room-permissions.test.ts`. Operator skills are shared by default (`~/.agents/skills` + harness skills dirs; explicit `sharedSkills` narrows), and codex/claude native web search is enabled in the isolated home (`agent-home.ts`); codex-acp receives its session MCP servers through request-scoped `session/new.mcpServers` (not persistent `config.toml`) and needs the capability primer on every turn. Model-written peer `@name`s become validated `mentionIds`, and explicitly addressed Room-agent messages may drive the receiving thin loop. `room-session.ts` owns both MCP mounts; `beeline-skill.ts` owns the primer; `monolith-room-turn.ts` owns mention routing; `reply-sanitizer.ts` removes harness startup chatter.
- **Shared attachments reach the harness as content, never as a URL it must fetch** (an unwrapped codex Room runs in Codex's offline read-only sandbox, so a `curl` of the public `/v1/media/<id>` fails there; do not make delivery depend on network). `attachment-delivery.ts` downloads each incoming message's files into the session `TMPDIR/beeline-attachments/<message-id>/` (the writable scratch overlay from `agent-home.ts`), names the local path in the prompt line, and adds ACP `image` blocks only when the harness advertised `promptCapabilities.image` (`AcpClient.canPromptWithImages`). Oversized or failed downloads degrade to an explicit line, never a failed turn. **Reaching the harness is only half the trip; pi drops the picture at its own config (C87).** An operator `models.json` custom-model entry (every OpenRouter agent has one — the key is fronted by an egress proxy) REPLACES pi's catalog record for that id and pi defaults a definition without `input` to `["text"]`, so pi's `transform-messages.ts` rewrites every image block to `(image omitted: model does not support images)` before the request leaves the process. `openrouter-routing.ts` pins the live `architecture.input_modalities` from the SAME endpoints listing onto `modelOverrides[<model>].input` beside the routing (pi applies overrides last); its cache stores `input` tri-state — array, `null` = the listing named none, absent = pre-C87, re-asked once. A picture rides inline only when the harness advertises the capability AND the pinned model takes images (`acceptsImages()`, both turn loops); otherwise `attachmentPromptLines` NAMES the reason so the agent says it in one plain sentence in the same turn. One inline image is bounded by `MAX_INLINE_IMAGE_BYTES` (a base64 block stays in session history and is re-sent every turn), and an ACP timeout carries `promptPayloadNote`'s image weight instead of bare "inactivity". Coverage: `attachment-delivery.test.ts`, `monolith-room-turn.attachments.test.ts`, `openrouter-routing.test.ts`, `agent-home.test.ts`, `acp.test.ts`.
- **`attach_file`'s legal roots must be the agent's WHOLE writable session home, not an enumerated subdirectory of it — the agent never picks where its harness puts a file it generates (C91).** #888 first widened `attach_file` past the checkout to `TMPDIR`, but a harness writes its own generated files into a sibling of `TMPDIR` under the same per-session `agent-home.ts` overlay (e.g. grok's `<agent-home>/grok/sessions/<cwd>/<session>/images/`), so that fix still refused exactly the file the agent just made. `resolveAttachPath` (`read-only-mcp.ts`) already takes an arbitrary root list with real-path/symlink-escape checks; both turn loops pass `config.agentHomeRoot` (falling back to `TMPDIR` with no per-room agent home) as the one scratch root, not a list of known harness subdirectories — the next harness will invent another layout. Coverage: `attach-file.test.ts`. **A Room can now create a file at all, not just read/attach one (C91 second half).** `write_scratch_file` (`read-only-mcp.ts`, beside `attach_file`) writes text or base64 content into that SAME scratch root (`BEELINE_ATTACH_SCRATCH_ROOT`) and returns a path `attach_file` can then send — never the checkout/worktree root, so a corner gains no way around its worktree. `resolveWriteScratchPath` mirrors `resolveAttachPath`'s real-path/symlink-escape checks but for a not-yet-existing file: it real-path-checks the deepest existing ancestor directory, then `mkdirSync`s the rest (freshly created dirs can't be symlinks). It gives an agent text/markdown/JSON/SVG, not a raster image — that still needs a converter, i.e. shell, which a Room does not have. The permission allowlist is untouched: both Rooms and corners already approve any MCP call to a mounted server. Coverage: `write-scratch-file.test.ts`.
- **Thin delivery stays server-native.** `monolith-room-turn.ts` reads server-stamped mentions, carries persona and attachment capability URLs into the harness prompt, keeps one request ID across draft/final/retract, and lets accepted turns drain before managed update restart. `phone-service.ts` stamps an implicit mention only when one agent is addressable; `agent-schedules.ts` posts scheduled work as the human creator's message when there is one; an agent-created schedule (creator = target agent) is instead authored by the hidden scheduler identity (`SCHEDULE_SCHEDULER_ID`, `@beeline/api-contract/scheduled-prompts`) as a system line `Beeline Scheduler ran a schedule for <agent> · <message>` mentioning the agent — a self-authored row would be dropped by the daemon inbox filter, and the body turn loop (`isScheduledPrompt`/`inboxItemTriggersTurn`) recognises it only by the inbox item's `systemEvent.verb === SCHEDULE_RAN_VERB` and prompts with the event's consequence (`inboxItemPromptBody`). Coverage: `daemon-api-client.integration.test.ts`, `reply-sanitizer.test.ts`, `apps/server/src/integration.test.ts`, `apps/server/src/agent-schedules.test.ts`.
- **Agent-to-agent turns carry their triggering inbox id** (`triggerMessageId`); `DaemonService.postRoomMessage` counts from that unthreaded trigger (human=0), caps the third agent hop by persisting the visible text with no delivery mention, and atomically settles a final reply's receipt. Coverage: `apps/server/src/integration.test.ts`.
- **Connect is the only onboarding flow, and it asks only what it cannot find out (C96).** `connect-command.ts` finishes app-authorized device pairing through `device-pairing.ts`, stages a monolith runtime, promotes the one-use exchange token, and launches the daemon. Runtime storage remains `${XDG_STATE_HOME:-~/.local/state}/beeline/agents/<pubkey>/`; confirmed removal moves it to `deleted-runtimes/`. **The wizard probes before it prompts:** a lone installed harness (`soleInstalledConnectHarness`) is selected without a list, then asked what it can run through the ordinary `loadConnectModelCatalog` with no provider and no key, bounded by `CONNECT_PROBE_TIMEOUT_MS` (12s; a configured goose answers in ~3s — one attempt, a timeout asks instead and never re-probes). Models the harness enumerated itself (`connectProbeFoundModels`: no `note`) prove it is authenticated, so provider, key AND model are skipped; anything else keeps the full flow verbatim, including the UNBOUNDED credentialed catalog read. Two rules hold this up: `modelCatalogProbeEnvironment` isolates goose into a disposable `GOOSE_PATH_ROOT` only when a provider was handed over (with none, the operator's config IS what is under test), and `agent-home.ts` COPIES `~/.config/goose/{config,secrets}.yaml` into the Room's `GOOSE_PATH_ROOT` — never a symlink, since goose persists a session's model back into its config. Coverage: `connect-command.test.ts`, `model-catalog.test.ts`, `agent-home.test.ts`.
- **The phone's MODEL / EFFORT rows read `agents.model_catalog` + `selected_*` verbatim; only the daemon fills the catalog.** `apps/body/src/model-catalog-sync.ts` (`syncAgentModelCatalog`, called from `cli.ts` after READY) probes the harness once per activation, posts `postAgentModelCatalog` with the selection sessions will use (server selection over runtime.json), and skips when `<runtime dir>/model-catalog.sha256` matches; failure is one log line. The phone renders both rows with or without a catalog (free-entry model id, generic effort ladder) and no explanatory prose. Coverage: `model-catalog-sync.test.ts`, mobile `MembersScreen.management.test.tsx`.
- **OpenRouter routing is pinned per model from live data, never globally, and uptime is only the FIRST pass.** `apps/body/src/openrouter-routing.ts` derives candidates from the public `/api/v1/models/<model>/endpoints` listing (tools + uptime ≥98% + advertised context, uptime order; bar drops to 95 when fewer than 2 qualify; #840's `deepinfra`/`novita` pair is the last resort), caches that 24h under `<runtimeDir>/openrouter-routing/`, then ASKS the survivors (C92): one small tool-enabled completion each, `allow_fallbacks: false`, keeping only providers that return non-empty text, ordered by measured latency — an empty 200 keeps a provider's uptime at 100% and never triggers OpenRouter's own fallback, so half an uptime-picked set can be silent. The probe is bounded (`OPENROUTER_PROBE_MAX_CANDIDATES`, parallel, short timeout, one in-flight probe per cache+model), cached separately (`<model>.probe.json`, `OPENROUTER_PROBE_CACHE_TTL_MS`), and NEVER blocks a turn: a cold probe cache serves the uptime order now and returns `decision.refresh` for the next activation (`connect-finish` is the one caller that awaits it). `prepareRoomAgentHome` writes the pin to `providers.openrouter.modelOverrides[<model>].compat.openRouterRouting` in the isolated pi `models.json` (pi applies overrides on every `getModels()`, so remote-catalog models get it too). Coverage: `openrouter-routing.test.ts` (recorded fixture in `fixtures/`), `agent-home.test.ts`.
- **Managed update remains functional.** `update-functional-probe.ts` proves a real ACP session/new plus turn without any corner/action tool; `managed-update.ts` records fleet proof; failure rolls back and leaves an operator-local alert. **The probe proves the bundle, not the model:** an empty turn passes (logged, `modelAnswer: 'unavailable'`) only when pi's own record shows an account/provider-side HTTP refusal (`isAccountOrProviderRefusal`: 401/402/403/407/408/429/5xx) or an empty model answer; a 400/404/422, no record, or recorded-but-undelivered text still fails (`update-functional-probe.test.ts`). **A 400/404/422 gets one appeal:** `compareWithCurrentRelease` spawns the previous release's bundle as `beeline update-probe --config <runtime.json>` (`current-release-probe.ts`; `EXTEND_TIMEOUT_USEC` buys the second probe) and passes as inconclusive only when that release is refused with the same status; a release without the subcommand reads `unavailable` and rolls back as before.
- **The OpenRouter pin never sets `require_parameters: true`**: OpenRouter answers pi's full knob set with 404 "No endpoints found that can handle the requested parameters" for every provider (2026-09-03, glm-5.3-flash: every Room turn and both probes); the `tools` filter already guarantees tool calling. GLM answers as `agent_message_chunk` text after `agent_thought_chunk`s; no reasoning-only fallback exists.
- **An empty ACP turn is never "no durable reply" as the only fact, and an empty completion is a ROUTING failure, not an answer.** pi-acp reports a pi turn whose assistant message was `stopReason: 'error'` (live case: OpenRouter 402, credits exhausted) as `end_turn` with zero content; `pi-turn-record.ts` reads pi's own session record (`$PI_CODING_AGENT_DIR/sessions/<cwd>/<ts>_<sessionId>.jsonl`, indexed by `$HOME/.pi/pi-acp/session-map.json`) and `empty-turn.ts` turns every empty turn into recovered text or a named reason (`provider error 402: …`, an empty model answer, or `describeEmptyTurn`'s stream shape for other harnesses). When nothing was recovered and pi named no refusal (`shouldRetryEmptyTurn`), both turn loops rotate the pin ONCE (`nextPinnedProvider`), re-activate on that ONE provider with fallbacks off, and re-run the same prompt; only then does the failed receipt carry `turnFailureReasonWithProvider`'s `… · served by <provider>`. The rotation lives for the session and is cleared on suspend; a corner checks turn told to say nothing never buys a retry. Coverage: `pi-turn-record.test.ts`, `empty-turn.test.ts`, `monolith-room-turn.failure.test.ts`.
- **Harness stderr is not a cause.** pi-acp 0.0.33 answers our ACP `notifications/initialized` with `-32601` and dumps it to stderr on EVERY session, then serves fine; that block used to be glued onto every timeout reason. `acp.ts` no longer sends the notification to a pi-acp harness (`isPiAcpHarness`) and `meaningfulHarnessStderr` drops the dumped block from both `AcpRequestTimeoutError` and the child-exit error, keeping anything else the harness wrote. Coverage: `acp.test.ts`.
- **Session plumbing remains shared.** `agent-home.ts`, `acp.ts`, `session-scheduler.ts`, model selection, persona delivery from `getAgentConfiguration`, bubblewrap read-only spawning, and the read-only MCP are the retained execution surface. Coverage: `daemon-api-client.integration.test.ts`, `thin-monolith.test.ts`, `room-session.test.ts`.

## Daemon self-update and CLI bundle

- **The public onboarding command is `npx usebeeline connect`.** `apps/body/src/connect-command.ts` owns the four-step brass wizard and hands off only to the installed canonical launcher; `apps/auth/src/server-agent-connect-routes.ts` owns the one-use browser device grant; the release's final `npm_publish` job consumes the completed `unified-release-index` version and publishes with OIDC provenance. **The product README has ONE canonical file, `packages/usebeeline/README.md`** (what npm publishes); the root `README.md` is a byte-for-byte copy written by `npm run readme:sync` — edit the canonical file, never the root copy, and keep every link absolute because the same bytes render from two directories. `README PARITY` (`checks.yml`) runs `readme:check`, `test:readme`, and `readme:badges`, which reads each badge's rendered `<title>`: a shields badge saying `repo not found` still returns HTTP 200 and a valid SVG. Coverage: `scripts/readme.test.mjs`.
- Authority `self-update.ts`, `self-update-manifest.ts` (ONE manifest seam), `self-update-cli.ts`. **`<prefix>/lib/beeline` is the ACTIVE BUNDLE ANCHOR** (symlink into `lib/beeline-releases/<id>` or a legacy real dir); forwarders derive `BEELINE_LIB_DIR` as the clean anchor path (no `pwd -P`, no `..`); `forwarderScript()` stays textually identical to install.sh's heredocs; detect with `lstat`; install.sh swings the anchor with rename(2), never `mv`. Stale pending journal → auto-rollback (`BEELINE_UPDATE_DISABLE=1` opts out). Coverage: `self-update-layout.test.ts`.
- **Supervised convergence is a 10-minute contract** (`managed-update.ts`: journal desired release + absolute deadline, quiesce, exit; `Restart=always` resurrects; the daemon never spawns its replacement; drain timers stay referenced). **Busy = a turn executing right now** (`RoomRuntimeCoordinator.activeTurnCount`; serving with no turn in flight is idle). `ManagedUpdateDrain` restarts an idle helper on the arming tick, a busy one at the earlier of turn end or the absolute deadline (own timer; forced restart cancels the turn; one `update restart waiting` line per minute). One failed successor probe rolls the SHARED anchor back for every helper (`rollbackFailedSuccessor`); helpers held by systemd restart backoff then come up on the old release — that, not a stuck drain, is the usual "not delivered" with idle helpers (v0.0.38). the daemon leg's promote waits 35 min with a per-minute readiness table. Coverage: `managed-update.test.ts`, `systemd-acceptance.test.ts`.
- `.github/actions/daemon-leg/` is the daemon build/promote leg of the unified release. It builds linux-x64 on the self-hosted prod runner and promotes the exact artifact to `relay-stack/web/dl/` (`scripts/publish-beeline-dl.mjs`, `docs/cli-bundle-channel.md`; darwin `enabled: false`). `npm run bundle:beeline` self-verifies via `scripts/verify-beeline-install.mjs`.

## Production host deploy

- `.github/actions/server-leg/` keeps the server artifact build but promotes the monolith with `flyctl deploy` to Fly app `beeline-server`; `unified-release.yml` confirms `server.usebeeline.app/readyz` plus the exact image identity from `/version` before daemon or OTA promotion. The retired relay-host deployment script has been removed.
- Two retained stack definitions: `relay-stack/` = isolated gate stack; `relay-stack/prod/` = retired production reference (its `README.md`). **One `materializer` process owns push delivery + repo-event ingestion** (`apps/push-gateway/src/index.ts`); the retired `beeline-events.service`/standalone gateway must not return. Coverage: `npm run test:deploy`.

## Mobile OTA release

- **One production release authority:** `unified-release.yml` + `scripts/unified-release.mjs` assign a human semver (starting `v0.0.1`) to one exact main SHA, build every artifact before the gate, then confirm server → daemon → app → device rehearsal. Component workflows are reusable legs only; mixed identities are NOT delivered. Coverage: `scripts/unified-release.test.mjs`, `mobileOtaRelease.test.ts`, `release-status.test.ts`.
- **A merge to `main` never releases.** `unified-release.yml` triggers on `workflow_dispatch` only (no `push`); `gh workflow run unified-release.yml` (no inputs releases current main head) is the sole way a release starts. The workflow's own `retry` job re-dispatches itself the same way via `createWorkflowDispatch` with `release_sha`/`release_version`/`retry_attempt`/`store_track`.
- **Three workflow files, no more** (`checks.yml`, `unified-release.yml`, `mobile-ota-rollback.yml`). In `checks.yml` a job's `name:` IS a required check context (`TYPECHECK`, `BODY SUITE`, `PUSH-GATEWAY SUITE`, `MOBILE SUITE`, `MOBILE-EXPORT REACHABILITY`, `PRODUCTION-CORPUS REPLAY`, `FRAME-BUDGET`, `STATE-UPGRADE`, `NATIVE FINGERPRINT`, `ACTIONLINT`, `Build auth container`, `Detect changes`): renaming one blocks merges. `PUSH-GATEWAY SUITE` runs `apps/push-gateway`'s own vitest suite (path-filtered on that app plus `packages/nostr`/`packages/api-contract`/`packages/buzz-client`); it is a PR gate only and is not part of `unified-release.yml`'s own confidence path. `unified-release.yml` calls its three legs as composite actions under `.github/actions/`, each exactly twice (build, promote). Coverage: `scripts/unified-release.test.mjs`.
- `.github/actions/mobile-ota-leg/` builds/promotes only as a leg of the unified release; `ota-release.mjs` owns beta/exact-group EAS operations, and `ota-delivery-index.mjs` owns pending → built → published → physical-device-confirmed state. Canary runs on operator `emulator-5554`; ledger records `previousProductionGroupIds`; a `beta-apk` binary is the ONLY canary vehicle (`EXPO_UPDATES_CHANNEL` baked at build). **The runtime version is PINNED BY HAND** (`runtimeVersion: "21"` in `app.config.js`): an installed binary keeps the stamp it was BUILT with, so `{ policy: "fingerprint" }` (v0.0.42) orphaned every installed app (`NoUpdatesAvailable`) — never restore a computed policy. The compatibility check lives at review time as the `NATIVE FINGERPRINT` gate (`apps/mobile/scripts/native-fingerprint.mjs` + committed `native-fingerprint.json`; `fingerprint.config.js` skips `extra`/versions/scripts/the pinned runtime and strips the channel header so every artifact of one commit stamps alike): a moved fingerprint under an unmoved pin FAILS with both platforms' old/new stamps and the bump instruction, `npm run fingerprint:write --prefix apps/mobile` re-records (refusing that same case; `--force` only for a `fingerprint.config.js` policy edit), and a bump means a new native build must ship. **Every stored update group is a platform→group map** (`candidateGroupIds`, `production.groupIds`, `previousProductionGroupIds`, index `published.groupIds`): platforms sharing a runtime share ONE group, platforms that do not get one each, promote/rollback republish per source group and prove the combined coverage, and a single group id reads as that group for both (`groupMapFrom`/`sameGroupSet`, `ota-delivery-index.mjs`). The canary (Android-only) reads the Android group and then parks printing the exact `eas build --profile beta-apk` command. Smoke reply waits start at phase markers (`publish-smoke-replies.ts`). Coverage: `mobileOtaRelease.test.ts`.
- **Store uploads are a leg of the release, never their own workflow.** `unified-release.yml`'s `store_track` input (`none` default; `internal`/`beta`/`production`) adds `store_android` (EAS `production` AAB from the release SHA, EAS-managed upload key — the committed sideload keystore is never used for Play — verified with bundletool, then `scripts/play-publish.sh` edit → bundle → `tracks.update` as a DRAFT → commit, then `scripts/play-publish-listing.sh`) and `store_ios` (`production-ci` profile = remote iOS credentials, ASC key written BEFORE the build, `eas submit`). Neither is a dependency of `delivery_report`, so a store failure never un-delivers a release. Play auth is the `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON` secret minted into a token by `scripts/play-token.mjs` (self-signed JWT at `oauth2.googleapis.com`; never `google-github-actions/auth`, whose iamcredentials exchange is disabled in the project; `play-token.test.mjs`). Moving a release between tracks stays operator-run (`scripts/play-promote-track.sh`). `apps/mobile/store/` is the reviewable listing source; `apps/mobile/fastlane/metadata/android/` is derived by `scripts/sync-play-metadata.mjs` (`--check` gates the store leg) and read verbatim. `PLAY_DRY_RUN=1` prints the API calls; coverage `npm run test:play`. Runbook: `apps/mobile/docs/store-submission.md`. `relay-stack/web/privacy/index.html` is served at `/privacy` by production nginx.

## GitHub identity and repository authority

- Authority `apps/auth/README.md`; `/auth/capabilities` gates GitHub on all five `BEELINE_GITHUB_*`; `npm test -w @beeline/auth`. Auth owns PKCE + a hashed one-use binding ceremony and never grants relay/Room/role authority.
- **A repo an admin does not OWN links via the owner's App install** (`owner_grant_needed` + `install_url`; durable `beeline_github_room_link_requests`; daemon parks transiently; mobile `OwnerGrantNeededCard`). **Repository identity is the immutable id; auth heals transfers** (`resolveGitHubRepositoryAccess`: grant → id-heal → alias → GitHub redirect). The binding's `githubInstallationId` pin is a hint, never a veto. Postgres parameter numbers must be contiguous.
- Org installs cannot be verified via `GET /user/installations` (trust the state-bound redirect; refuse only on definitive denial); `reconcileGitHubInstallations` enumerates with the App JWT. Error handler never emits a silent 500. Install/update returns share the OAuth callback (`completeGitHubInstallation` first). Native returns have one authority `github-auth-session.ts`. New-key onboarding never persists before `seen` AND `confirmed`. **Repo event ingestion is a materializer consumer** (`events-service.ts`, `hosted-events.ts`); `beeline events daemon` must not return.

## GitHub key succession

- **Roles resolve succession-aware at the roster funnel** (`communityMembers(ctx, id, {predecessors})` → `inheritRolesThroughSuccession`; `BuzzClient` fetches `/auth/oidc/predecessors` once). Ledger `beeline_key_successions` (`apps/auth/src/store.ts`; helpers `resolveCurrentPubkey`/`sameIdentity` only). Room-token authority resolves the binding author to its CURRENT key; binding resolution follows the chain only through `apps/auth`'s injected resolver.
- **Zero re-invite migration is client-side** (`migrateSuccessorMemberships`; DMs/corners skipped); the production relay honors member-adds only from a room ADMIN → `MembershipProjectionTimeoutError` = NOT MIGRATABLE (`unmigratable-rooms.ts`).

## Mobile client (Happy fork)

- `apps/mobile` is vendored Happy, isolated from root workspaces (`npm run mobile:install`); `@beeline/*` via Metro aliases to `dist/` (`metro.config.js`, existence-guarded). One identity `app.usebeeline.mobile`/`beeline://`, one channel `production` (`appBranding.test.ts`); no `googleServicesFile`. Release APK `npm run apk:release`; `npm run e2e`. **BuzzRigTransport is write/live-only**; reads go through `RoomViewClient`; MMKV holds only scoped DTO responses + outbox. RN-mocked tests read `.props.children`, never `.children`.
- **Corner status renders the indexed remote fact verbatim.** Mobile shows working, PR open, checks failing, unknown/degraded, or done from `corner-remote-state.ts`; it has no approval/review projector or merge controls. Corner agent identity still resolves through the Room surface.
- **Pinned corner line and thinking indicator are independent** (`selectPinnedCorner`, `room-indicators.ts`: any non-terminal corner is a candidate, gold/pulse for `live` only); `TurnProgressLine` from `latestAgentTurns` alone; its glyph is `BeelineMarkSpinner` (the shipped mark's path, stroke-only, in a fixed 18px cell — the one drawn-motion exception, `DESIGN.md` → Motion), never a cycling text glyph. Corner status is NEVER in the transcript (`CornerLiveBar.tsx` above the composer; `HullLivePulse`, never a sweep). **Leaving a corner is a lookup** (`corner-navigation.ts` `chatBackAction`; pushes via `cornerHref`). Header title via `channelHeaderTitle` (`null` → skeleton).
- **The foreground never blocks on the network** (`room-entry.ts` fan-out — new reads are independent `step()`s, never chain links); `no-foreground-blocking.test.ts` whitelists awaits and bans `cacheLiveSessionEvent` (singular). Live events coalesce per frame; projections identity-stable (`projection-identity.test.ts`); `renderItem` deps take `onlineVerdicts`/`useStable`, never raw presence. **Transcript FlatList is `inverted`** (`maintainVisibleContentPosition { minIndexForVisible: 1, autoscrollToTopThreshold: 50 }`; scroll rule in `buzz/room-scroll-follow.ts`, held while a drag/momentum is in progress: one `scrollToOffset(0)` per new arrival, PLUS one when the composer/keyboard footprint shrinks (a send collapsing the composer or dropping the keyboard) while already pinned to the tail — C97, since neither case gets a new row id or a big-enough `autoscrollToTopThreshold` hit; `marginBottom` is the gap ABOVE). Ledger leaf components stay `React.memo`'d.
- **Room-list deck is server-indexed** (#566/#567/#577/#598 superseded the old client-side `room-list-row.ts` logic, which now only supplies title formatting). `channels.tsx` renders `ChatListView.chats` from `RoomViewClient.chats()` (both `PhoneService.readChats` and the local-stack `CHAT_LIST_SQL` carry `directMessage.peer` for a DM row; `agentState?: 'needs-you'|'working'` is the max-severity rollup over corners + the Room's latest `agent-turn`). **Row facts have one derivation** (`room-list-row.ts`: `roomRowName` sigil `@peer`/`#room` + tile seed, `roomRowPreview` `you:`/`@handle:`/`No messages yet`, `roomRowNeedsAttention` = unread OR needs-you lighting the one 7×7 brass square; `tile` only for a DM row — a Room row has none and leaves the 40px leading unit empty so the copy column stays straight, C71) — never an inline ternary, no NEW badge, no deck circle (`DESIGN.md` → Index rows). Unread is room-level (`room-read-state.ts`, marked on return only). **Exactly ONE home surface** (`channels.tsx`; Personal Workspace differs only in `allowPeopleInvites`). Settings gear → `/beeline/settings` hub. Previews sanitized where stored (`roomPreviewText`, no SDK import). Coverage: `room-indexer.test.ts`, `room-list-row.test.ts`, `channels.design.test.ts`.
- **A removed Room needs two local layers** (`removed-rooms.ts` tombstone + `removeChannel`); archive (kind:9002) ≠ Delete (kind:9008 `deleteRoom`); tombstones only for self-leave/legacy. **A visible control must act or explain itself** — no `if (!transport) return`; Members paints from cache (viewer added back — the roster cache omits the viewer), empty presence is UNKNOWN, handlers await `requireConnection()`. **Direct Room membership is the complete roster** (`room-participants.ts`, never intersected with Workspace rosters). Coverage: `MembersScreen.management.test.tsx`, `RoomRosterSheet.test.tsx`, `MemberPickerSheet.test.tsx`.
- **Members is one word and one picker.** No members glyph (`members-glyph.test.ts`). `MembersScreen.tsx`: "Members" over counted `People N`/`Agents N` heads; rows are name + `@handle · role[ · online|offline]` (an agent's presence is that lowercase word; the ring means working, C77); role/removal live on the row's opened detail; the one brass action (`BrassButton`) opens `MemberPickerSheet.tsx`, the ONE picker, mounted by the Room too. **A Room picker's candidates are the WORKSPACE roster minus Room members** (`RoomViewClient.workspace()` on open; filtering the Room's own list lists nobody, C74); a NEW agent is the pairing command (`action=add-agent`). **A Room has ONE way into its members** (C83): the chat header carries no `+` — `N members ›` opens `RoomRosterSheet.tsx`, whose section heads own the add control (a manager keeps an EMPTY head in a top-level Room so an agentless Room can still reach one). The picker's empty line names which fact it is (`memberPickerEmptyLine`: all peers of that kind already here vs. no peers at all — it needs `workspacePeerCount`, not just `addableCount`). `RoomRosterSheet.tsx` matches Members (ring from `workingByPubkey` only, no status square, C76). Server `removeWorkspaceMember` = `addWorkspaceMember`'s ladder, every live Room, `member-removed` lines; agents stay `removeAgent`. Coverage: `MemberPickerSheet.test.tsx`, `chat.member-picker.design.test.ts`, `MembersScreen.*.test.tsx`, server `integration.test.ts`.
- **Agent names** (`agent-display.ts`: soul → registered `displayName` → `fallbackAgentName`; rosters merged field-wise, soul never dropped, empty roster never committed; `resolvePendingAgentDisplay` null until hydrated). Two agent messages never share a bubble (`upsertChatMessages`, draft→final only); `isNew` never persisted, reveal consume-once (`message-reveal.ts`). Coverage: `agent-display.test.ts`, `local-cache.test.ts`.
- NIP-05 (`nip05.ts`, never throws); managed `@usebeeline.app` identity owned by `apps/auth` (`/.well-known/nostr.json`, `/nip05/claim`; both nginx configs proxy; GitHub ownership wins). `RepoPicker.tsx` is self-bounding (never wrap in a scroll container). Corner objective via `cornerObjectiveLine`, never free-text (PR #165's wire must not return); a corner's PR/test state is ONE inscribed link line (`corner-status-line.ts`, `CornerStatusLine.tsx`), never a check list or merge button. Souls: `setAgentSoul` (human-signed, no authority); a soul author is not the agent (`agent.ts` rejects keys with a human kind:0). Workspace exit `leaveCommunity` (sole owner refused client-side).

## Android push notifications

- Production mobile registration/test/unregister and OTA receipts use typed monolith phone operations at `server.usebeeline.app`; legacy `https://usebeeline.app/push` is only the non-monolith fallback. Successful void operations are HTTP 204 and clients must not JSON-decode them (`server.ts`, `monolith-operation.ts`; coverage: `integration.test.ts`, `monolith-operation.test.ts`). **Attention-only defaults** (explicit `p` mentions, DMs, waiting-on-human transitions). **The toggle reflects REGISTRATION truth** (`buzz-push-status.ts`; token fingerprints only in logs). Foreground policy `foreground-policy.ts` (suppress when active/open Room; never unconditional `shouldShowAlert: true`). At-most-once delivery `delivery-state.ts` (attention keys cleared by idle/concluded/closed, not `working`). Legacy gateway ops: `apps/push-gateway/README.md`.

## Community model

- Authority `community.ts` (`community.live.test.ts`): NIP-29 group, self-referencing `community=<h>` on kind:9007, 9000 roles, 39001/39002 projections. Workspace membership = direct membership in every live top-level Room for people; a rollback-aware agent-pairing claim atomically adds the new agent to its Workspace and the inviter's eligible current top-level Rooms, while legacy claims stay Workspace-only and later per-Room attachment remains explicit. Invites are signed kind:30078 (`t=buzz-community-invite`, `d=SHA-256(token)`, `findCommunityInvite`). DMs (`direct-message.ts`): deterministic private `t=buzz-dm` Room; agent DMs strictly conversational — never borrow a repo or open a corner.

## Explicit external MCP capabilities (Trusty Squire)

- `external-mcp-capabilities.ts`: records store profile names only (`squire-credential-use` / `squire-app-access`, disjoint, `--access creator`, codex/claude only). Pairing runs `npx @trusty-squire/mcp connect`; daemon mounts use `server`. Squire is the one governed hole in the read-only boundary: inventory read-only; `use_credential`/`grant_app_access`/`revoke_app_access` need P1's Tier-2 `operation.execute` envelope (owner signature, one use, receipts; `grant_app_access` needs an hourly cap); host broker reverifies; Squire on a Body-owned file-only XDG store masked from agents (`squire-host-broker.test.ts`).

## Grok Mono Hull UI theme

- **Single source of truth: `/DESIGN.md`.** Tokens `buzz/groknight.ts` (grayscale, one accent `#d7af5f`, `radius: 3`; `diffAdded`/`diffRemoved` are the one chroma exception). A box wraps only something the user must act on or a non-repeating region; repeating units get whitespace/`hairlineDivider`. Primitives `MonoHull.tsx`; marks `brand.json`/`beeline-mark.json`.
- **Type has four sizes and one lint.** `typeRoles` in `apps/mobile/sources/buzz/groknight.ts` (`hero` 22, `body`/`bodyStrong` 16, `meta` 13, `sectionHead` 10 tracked uppercase, `machine` 13 mono; `space`/`layout` beside them; all on `theme.buzz.type/space/layout`) are the only text sizes a screen sets. `sources/buzz/calm-lint.design.test.ts` holds every `sources/**/*.tsx` to its count of raw `fontSize:`/`letterSpacing:` literals in `apps/mobile/design/calm-baseline.json`; counts only shrink, and a surface PR that removes raw values regenerates the baseline (`CALM_BASELINE_WRITE=1 npx vitest run sources/buzz/calm-lint`). Roles pinned in `groknight.test.ts`; DESIGN.md → Type.
- **Rooms and Corners render ONE ledger primitive** (`Ledger.tsx`, one `renderItem` branch). ONE message size; a user message is NEVER bolded/enlarged/inset — ownership reads from the byline; corners attribute like Rooms via `ledgerSpeakerKey` (`ledger-attribution.ts`, per run). Pinned by `transcriptSurfaces.test.ts`. Fonts IBM Plex Sans/Mono + Bricolage Grotesque (no `SpaceMono`); mono allowlist `Typography.test.ts`.
- Streaming lanes settle to one durable `agent-message`; only classified failure/merge/consequential action survives as one room-update line (`activity-timeline.ts`); no reply echo under agent turns; `HullMechanismReveal` pops only live-mounted rows (`MechanismMotion.test.ts`).
- **An expanded machine run is ONE line per call, and the command is the object** (C88, DESIGN.md → The ledger). `buzz/tool-call-row.ts` alone decides a call's verb/object/outcome/duration/output: command over harness title (a harness labels a directory listing "Reviewed the current changes"), middle truncation, transport-only output (`[{"type":"terminal","terminalId":…}]`) renders NOTHING, a failed call opens itself. `ActivityTimeline.tsx` only draws it — success shows no outcome word, `failed` is `diffRemoved`, `running` is brass, and the command is never printed twice. `redactPaths` (`activity-timeline.ts`) collapses a WHOLE path including its bare head; matching only the slashed tail produced `node_modulesprettier`. Coverage: `tool-call-row.test.ts`, `ActivityTimeline.test.tsx`.
- **The alive ring paints `ALIVE_RING_PAD` (`buzz/identity-mark.ts`) outside the tile's layout box; an ancestor that clips at the tile's edge must outset by it** — gesture-handler's `Swipeable` container is `overflow: 'hidden'` at the transcript row's content edge, so `SwipeToReply` (`RoomMessageVariants.tsx`) outsets its `containerStyle` and pads `childrenContainerStyle` back by the gutter (C70; `IdentityMark.test.ts`, `RoomMessageVariants.test.tsx`).
- **Identity marks are ONE component** (`IdentityMark.tsx`): people and agents are Speakeasy's twelve creatures (`buzz/faces/`: static ports, `recolorEdge` edge layer, `defaultFaceForSeed` FNV-1a-into-12; `RoomViewIdentity.face` overrides; tile renders `face ?? defaultFaceForSeed(pubkey)`), person = hue creature on ink plate, agent = ink creature + bone lens band on hue plate; only `IdentityMark.tsx` composes `buzz/faces`. `identity-mark.ts` keeps the curated `HUE_WHEEL` (name in the byline is set in that hue at body size) and the Workspace 3×3 brass plate (fill/cypher are Workspace-only now); **gold ring = working agent only** (`selectWorkingAgents`, `room-indicators.ts`: this channel's fresh working receipt or a live corner's agent; the presence lease is never an input — C77, a helper whose turns all fail still renews it; Workspace Members has no turn signal and draws no ring). `IDENTITY_FILL_STATES`/`FACE_IDS` order stable; legacy `components/Avatar.tsx` family is a named exemption. Evidence regen: `apps/mobile/docs/evidence/faces/README.md`. A status is inscribed, never framed; persistent chrome carries no `HullSurface`; one glyph per concept (`vocabulary.ts`); wordmark `beeline.` with brass period. Android: a `flex:1` Text wrapping only Text inside a row View goes blank (`MonoMarkdown.test.ts`). Diff view: vertical-outer/horizontal-inner ScrollViews, plain Text rows.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise. Stay under 40KB — it is loaded into every agent request.
