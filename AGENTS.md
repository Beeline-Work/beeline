# Project agent memory

Committed home for project-intrinsic agent knowledge: rule + authoritative file + coverage pointer. The named code and tests hold the detail; do not re-expand prose here.

## Core invariants (read first)

- **kind:30078 reads filter by `#d`, NEVER `#h`** (a `#h` filter over kind 30078 matches nothing). `agentPresenceKey(channelId)` (`packages/buzz-client/src/agent-presence.ts`) is the one presence-key builder; `Body.missingChangeReviewUnits` enumerates exact `#d` coordinates. Generic relay stubs cannot catch this — use `matchesFilterRelayFaithfully`/`stubRelayFaithfulPublishing` (body.test.ts). Source assertion: `agent-presence.test.ts`.
- **Production relay may answer a multi-value `#h`/`#d` filter with matches for only one key.** Every fan-out in mobile transport and `packages/buzz-client` expands to one single-key filter per key inside ONE batched `query([...])`/subscription; fake relays answer multi-key filters first-key-only (`buzz-rig-transport.test.ts`, `client.test.ts`).
- **Local stack vs production:** `repository-truth.ts` maps `git://` markers to `https://`, unusable against the local docker stack (`relay-stack/compose.yml`, http 127.0.0.1:3010) — pair repo-less and invite top-level Rooms. That stack includes a credential-free materializer, so local RoomView reads stay on 3010. Mobile's `getEffectiveRelayUrl()` honors the embedded runtime URL when no device override exists, and `BuzzClient` treats loopback/Android-host relay URLs as the intentionally open local relay instead of waiting forever for a NIP-42 challenge; coverage: `buzz-identity-storage.test.ts`, `client.test.ts`. Gate helpers resolve the relay from env AT IMPORT TIME: set `BUZZY_RELAY_HOST=127.0.0.1:3010`, `BUZZY_RELAY_SCHEME=http`, `BUZZY_RELAY_URL=http://127.0.0.1:3010`, `BUZZ_RELAY_URL=ws://127.0.0.1:3010` BEFORE importing `@beeline/gate`, or reads/writes silently go to PRODUCTION.
- **Private Workspace pairing does not require a relay-wide membership gate.** `visibility='invite-only'` maps to NIP-29 `private`; an outsider redeems the globally readable kind:30078 marker through the authenticated `/agent-pairing/claim` server boundary, which atomically verifies the marker's current member author and reserves it for that exact key before the client publishes canonical kind:9000 membership. Acceptance proof: `npm run prove:private-pairing` against the ordinary open local relay.
- **Hand-testing `beeline start`/`pair` in a worktree of this repo can start a REAL production daemon** (runtime pointers live under the shared git common dir). Always pass an isolated `XDG_STATE_HOME` (and `--repo` to a throwaway repo for `pair`).
- **A corner's self-signed kind:30078 `buzz-corner-state` record is agent-authored ephemeral fact, not durable truth — it must be membership-gated like every other agent-authored read.** `removeAgent`'s per-Room removal is best-effort (an unarchived corner can straggle), so a still-open corner-state lease can outlive its author's eviction; nothing retracts another key's already-published event. Fixed at all three `apps/push-gateway/src/room-indexer.ts` reads (`corner_states` CTE, the standalone `'corner'` section, the Room-nested `'sibling'` section) by joining `channel_members … removed_at IS NULL` on the corner's own `channel_id`, matching `latest_agent_turns`. Coverage: `room-indexer.test.ts`'s "never renders a removed ghost agent as thinking/working" case.
- **Retired daemon prose has one tombstone authority; a shape carrying variable data (a path, a model name) gets a STRUCTURAL matcher, not an exact-set entry.** `packages/buzz-client/src/retired-agent-notices.ts`'s `isRetiredAgentNotice` (exact-set match, plus bounded regex shapes: the attachment-delivery ENOENT dump, the model-unavailable startup wall), applied at three reader floors — `RoomIndexer.projectEvent`, `room-list-row.ts`'s stored-preview check, and mobile's `retired-agent-notices.ts` cache scrub. Coverage: `retired-agent-notices.test.ts`, `room-list-row.test.ts`, `surface-storage.upgrade.test.ts`.
- **A mobile fix must not depend on a brand-new `@beeline/buzz-client` export** (Metro resolves built `dist/`; a missing symbol inside `.catch` fails closed silently) — add a `testID` and grep the bundle. `apps/auth` tests also import `dist/`: rebuild `@beeline/nostr` and `@beeline/buzz-client` after SDK edits.
- **Deleted features stay deleted:** daemon health chat (`agent-state-messages.ts`, five sentences; `agent-state-messages.test.ts` is the TOMBSTONE), the client "X seems offline" notice (`transcriptSurfaces.test.ts`), photo overrides (`PHOTO_OVERRIDES_ENABLED` ships `false`, `photo-overrides.ts`; `identity.photo-darkflight.test.ts`). A down agent is shown only by presence lease, corner/turn state, daemon log. Sole notice exception: the tagged `buzz-agent-model-unavailable` startup line (`publishModelUnavailableState` dedupes across restarts) — never grow it to relay/rate-limit/repo failures.

## Server-indexed surfaces and Room reads

- **Server-indexed surfaces are the only durable Room/Workspace read authority.** `apps/push-gateway/src/room-indexer.ts` owns paint-ready queries; `packages/buzz-client/src/room-view.ts` names DTOs + signed client; screens render validated responses verbatim. A relay signal only marks a surface dirty; only verified draft/thought/presence events enter `LiveOverlayDecoder`. Never restore a client parser/reducer/selectors/Workspace snapshot. Typed kind:9 events are non-conversation unless explicitly marked; `Body.agentHistory` consumes only `presentation='message'`. Coverage: `surface-runtime.test.ts`, `room-indexer*.test.ts`. A Body test whose fixture only models relay `/query`/publish must not exercise `agentHistory`/`indexedRoomMessages` (a real authenticated `GET /room/:id`) — stub it directly (`stubEmptyAgentHistory` pattern, `body.test.ts`); an upload flow needs `mediaUploadResponse` (`relay-test-helper.ts`), never `JSON.parse`d like an event publish. `apps/body` is CI-gated on PRs (`.github/workflows/body-tests.yml`).
- **Turn progress is server-indexed receipt lifecycle** (`RoomView.latestAgentTurns`; mobile lights a fresh `working`, clears on `complete`/`failed`/contradiction/90s horizon). Draft/thought overlays never drive the thinking indicator; the local pre-receipt "buzzing…" ack (`selectComposerAckState`) is a separate, earlier stage that the real receipt always supersedes. **Workspace pictures are current kind:39000 metadata**, never only the kind:9007 create.
- `surface-storage.ts` persists only response + outbox partitions; cold open installs invalidation first, then exactly one signed GET. **Read marks are server-owned** (`beeline_room_read_marks`, `apps/push-gateway/src/database.ts`); never add a local read-mark store. A cold Room-list bootstrap must reach a terminal UI state (`channels.tsx`).
- **A NIP-10 reply parent is an opaque same-Room proof** (`messageSubmitReply`; `reply-command.ts` alone owns structural tags). `relay-error.ts` is the sole interpreter of publish refusals (`RelayPublishError`, raw text out of `message`; 408/429 retryable). Coverage: `relay-error.test.ts`, `publish-delivery.test.ts`. **A ledger entry carrying a reply-reference always re-announces its speaker** — run-continuation byline suppression never applies to a reply; ambiguity resolves toward showing the byline (`ledger-attribution.ts`).
- **One presentation authority for floating surfaces:** `ModalManager.ts`; `HullDialog.tsx` owns the sole RN `Modal`; `HullActionSheet.tsx` the sheet. Direct `Alert.alert`/system sheets/native menus prohibited (`native-dialog-enforcement.test.ts`). Its `KeyboardAvoidingView` must come from `react-native-keyboard-controller` (never RN's own — the app hands keyboard tracking to `KeyboardProvider` app-wide in `app/_layout.tsx`, and RN's own view never learns an already-open keyboard's height at mount, snapping late when a sheet opens over a focused composer); Android behavior is `translate-with-padding`, never `height` (`keyboardAvoidance.test.ts`).

## Branch conventions

- **Updating a corner branch to its target is standing authority** (fetch/merge/rebase, resolve, rerun checks, commit — no second permission; never land the target). `Body.publishMergeReady` publishes committed feature work without checking target freshness; a signed merge press that cannot fast-forward keeps its standing approval and runs one exact-tip `target-sync` continuation (`github-delivery.test.ts`).
- `fm/*` branches stack; check `gh pr list --json baseRefName,headRefName` before assuming a file is on `main`; `fm-integration-verify` ↔ `origin/fm/buzzy-integrated`; `git branch -a --contains <commit>` finds the source branch.
- Cherry-picks off `feature/*` can omit a declaration referenced only as context — `npm run typecheck` catches it. Two branches adding trailing params to one function: match slots to call sites on `origin/main`. Merging `origin/main` into stale `fm/*` conflicts mostly on `relay-stack/web/dl/` artifacts — pick either, regenerate via `npm run bundle:beeline`. Interleaved `describe(...)` conflicts: rebuild siblings from `git show <sha>:<path>`, verify with `npx esbuild`.
- **Stacked PRs merged base-first do not propagate** — re-target to `main`; verify with `git grep <symbol> origin/main`.

## Push credential boundary (ref-policy broker + sandbox masks)

- **An agent can NEVER land on main without an owner-signed protected-ref authorization — structural, any GitHub plan.** (1) `buildAgentEnv`'s `REPO_PUSH_CREDENTIAL_ENV_DENYLIST` (`apps/body/src/config.ts`) strips `GH_TOKEN`/`GH_ENTERPRISE_TOKEN`/`GITHUB_TOKEN`/`SSH_AUTH_SOCK` LAST, after any passthrough. (2) `bwrap-sandbox.ts` masks `~/.config/gh`, `~/.config/trusty-squire`, `~/.ssh`, `~/.netrc`, `~/.git-credentials`, `~/.secrets.env` + `BUZZY_BODY_SANDBOX_MASK` with tmpfs/`/dev/null` in BOTH modes (after the ro-bind, before writable binds). (3) Every daemon push goes through `performBrokeredPush` (`push-broker.ts`): feature branch allowed; protected ref only with a verified owner-signed corner approval (`findHumanMergeApproval`) or the distinct `mission-standing-land` authorization; audit line `[body] push-broker:`.
- Residual (docblock `bwrap-sandbox.ts`): no mask list enumerates every secret on a shared machine; docker.sock/systemd user bus pierce the sandbox by owner choice; recommended posture is a credential-less user account (not built).

## Gate / live security tests

- Authority `spec.md` (Failure modes → Agent in push-rights); gate `apps/gate/`; live `cd apps/gate && npm run test:live` after `npm run stack:up`; proof `npm run prove`. Relay defaults `relay-config.ts` → `usebeeline.app` (`relay.buzzrouter.com` permanent alias). Room tenancy is server-stamped `channels.community_id` (`AuthStore.relayCommunityIdForRoom`); the kind:9007 `community` tag is NOT the tenant boundary.
- GitHub sign-in: `buzz-identity-storage.ts` seals the candidate key pre-browser; `github-auth-session.ts` persists the challenge; `/auth/oidc/bind` create-once, `/auth/oidc/recover` explicit replacement (`onboarding.github-callback.test.ts`).
- Provisioning `apps/gate/src/provisioning.ts`. Worker checks the `#t=buzz-agent` registry before roles; managed/remote reviewer keys refused (`agent-identity.live.test.ts`).

## Body (@beeline/body)

### Tools, homes, skills

- **Agent actions and truth reads cross one typed boundary:** `agent-tool-contract.ts` (`read_mandate`/`read_corner`/`list_corners`/`request_mandate`/`open_corner`/`close_corner`/`schedule`/`deliver`), `authorize-or-request.ts`, `agent-tool-host-broker.ts` loopback. The broker must dispatch independently-addressed MCP calls concurrently so a truth read cannot queue behind a slow corner provision. Pi uses generated `pi-mcp-session.ts` + the pinned adapter (`MCP_DIRECT_TOOLS` barrier; never `pi install`); inventory mismatch fails closed.
- **Managed-update READY is functional and fleet-wide:** `update-functional-probe.ts` proves session/turn/`read_mandate` before `managed-update.ts` records proof; failure restores the previous release, pins the publish, alerts once; `beeline update --clear-pin` re-enables (`update-functional-probe.test.ts`).
- **Codex Rooms:** `agent-home.ts` always writes `[agents] enabled = false` in the per-Room `CODEX_HOME/config.toml`.
- **Agent-home skills are an exact allowlist:** `using-beeline` + `mission-brief` + `runtime.json` `sharedSkills` (`beeline pair --share-skill`), copied 0600 from `~/.agents/skills` after rejecting traversal/links/credential names; the generated home owns `$HOME`. `buzz-readonly-mcp.read_agent_file` is the only auto-approved Room self-read. Pi: only `~/.pi/agent/models.json` copied; inline `apiKey` never exposed (`agent-home.test.ts`).
- **Ordinary operator skills/MCP are shared:** `~/.claude|.codex|.grok/skills` linked; user-scope MCP declarations copied 0600 per activation (Squire filtered). Claude `settingSources: ["user"]`, never `strictMcpConfig`. Corner extras from `<runtimeDir>/operator-mcp.json` (creator-gated; Rooms never). pi ignores handed `mcpServers` (`harness-tool-scope.test.ts`).
- **`using-beeline` skill** (`beeline-skill.ts`): materialized per harness with a release stamp; primer in every session prompt (Claude systemPrompt; Codex/Grok/Pi turn prefix). Authority-free; `BEELINE_SKILL_PROMPT_LINE` added once — never grow the prompt.
- **Persona** (`persona-instructions.ts`) writes native instruction files in the isolated home; fallback `_meta.systemPrompt`/turn prefix. **Agent memory** (`agent-memory.ts`): per-(agent, workspace) via `memoryScopeKey`, writable in Room and corner (only path-resolved edits permitted); `MEMORY.md` is the only standing-directive store. Persona scratch goes in `agent-private-state.ts`'s root; `publishMergeReady` ignores only that exact symlink.
- **A saved `buzz-agent-soul` edit is applied at session ACTIVATION only**, so a warm session (any still-live session; grok/pi warm-hold up to 30min) would otherwise keep the persona it started with indefinitely. `RoomRuntimeCoordinator.refreshPersonaIfSoulChanged` (`room-runtime.ts`) detects a newer soul `updatedAt` on the `reconcile()` heartbeat and calls `Body.refreshPersonaForSoulUpdate`, which `scheduler.suspend()`s every idle session it manages so the next turn re-activates with the fresh soul — `suspend()` is already a no-op mid-turn, so this never interrupts a live turn. Coverage: `room-runtime.test.ts`'s "agent soul freshness" suite, `body.test.ts`'s "refreshes a warm session persona on the next turn after a soul is saved".
- **Per-room harness state, shared auth** (`agent-home.ts`: `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/XDG/`TMPDIR` → `<roomRoot>/agent-home`; `BUZZY_BODY_ROOM_HOME=1/0`). **`buildAgentEnv` is the child's entire environment** (`BUZZY_BODY_AGENT_ENV_PASSTHROUGH` extends; `BUZZY_BODY_AGENT_ENV_INHERIT=1`). **Bundled ESM helpers execute through the active-release symlink** — compare `import.meta.url` with the realpath of argv (`squire-mcp-proxy.ts`).

### Daemon core, Rooms, relay

- **`ThinDaemonCore` (`thin-core.ts`) + `RoomRuntimeCoordinator` (`room-runtime.ts`)**; contract `docs/daemon-supervision.md`. Out-of-turn Git stays behind `apps/gate/src/git.ts`. `reconcile()` never crash-loops and is wake-driven (kinds 9000/9001 `#p=agent` + 60s heartbeat). Removal is tri-state — exceptions are `unknown`; removal only after consecutive confirming reads; `removeAgentRuntime` renames to `<state-root>/deleted-runtimes/` (recoverable). **An archived Room is terminal** (`terminal-inert`; returns only after a fresh NOT-archived read). Coverage: `room-runtime.test.ts`.
- **A relay drop never ends in a dark daemon:** poll backoff cap 30s, never attempt-capped; `runRoomPushLoop` registers close/abort observers before subscribe awaits; `ws.ts` reconnects on post-connect `error`; `isDurableRoomJoinFailure` parks durable 10min, transient 30s. Every retry/tick is jittered (maintenance phase-shift ≤ half a tick); relay `retry in Ns` is never jittered downward. Coverage: `ws.test.ts`, `presence-truth.test.ts`.
- **Room delivery is WS-push** (`runRoomPushLoop`; HTTP backstop) and must call `onRoomPollSuccess` for the life of a healthy socket (`room-ws-push.live.test.ts`). One shared socket per daemon (`relay-socket.ts`; `acquireRelaySocket()` → `release()`, never `disconnect()`). `presenceCaches` cannot reuse `roomSockets`. Write-permission decisions: Room WS first, poll backstop.
- **Reads:** `http.ts` retries 5xx/transport; `query()` (`query.ts`) is WS-primary with HTTP fallback — `wsQueryEvents` must unsubscribe on every exit path; `waitUntil*` helpers are WS-primary with a poll backstop. `findCommunityInvite` and `apps/gate` stay HTTP-only. HTTP is NIP-98-bound.
- **`beeline start` RESTARTS a running daemon** (`start-command.ts`: graceful drain, `/proc/<pid>/cmdline` identity, fails closed). Runtime root `${XDG_STATE_HOME:-~/.local/state}/beeline/agents/<pubkey>/` with a link pointer under the git common dir (nothing moved). **Repository keys are relay identities** — `repository-path.ts` makes PATH-safe segments; never join a raw key into a path.
- **Recurring work** (`work-calendar.ts`): never owns ACP slots; receipts observability only; occurrences enter the Room dispatcher with `trigger='schedule'` after rechecking authority; the schedule is the full mandate; delegation/corner directives inert. **Mission authority is one attenuation of the permission ledger** (`permission-request.ts`, `mission-authority.ts`, `mission-script.ts`, `push-broker.ts`; `apps/body/README.md`). **Every model call requires `ModelTurnAttribution`**.
- **Access policy** (`access-policy.ts`): `creator` default, `everyone` legacy fallback, `allowlist`; unknown sender → NOT permitted. **Multiple agents = multiple daemons** (identity reuse refused before consuming the code). **`beeline pair` never leaves a half-created agent** (preconditions before redemption; `abandonAgentPairing` on failure; no rollback past `writeRuntimeRecord`). `--repo <path>` is the ONLY way a repo is recorded at pair time (`runtime.test.ts`, `pair-cli.test.ts`). **Pairing redemption auto-attaches the agent to every top-level Room the inviting user belongs to** (`attachAgentToInviterRooms` in `packages/buzz-client/src/agent.ts`, DM/corner/archived excluded via `communityRoomIds`) — the app never offers a Room picker. `beeline pair` always mints a fresh identity by default (ignores ambient `BUZZ_AGENT_KEY`/`BUZZ_PRIVATE_KEY`); `--use-env-key` is the sole opt-in to reuse the ambient key.
- **Clack CLI:** at most ONE spinner live, never under a prompt — ask everything first, spin last (`pair-cli.test.ts`); `daemon` always plain.
- **Soul authorship follows key succession; startup fails closed without the resolver** (`resolveCurrentIdentityPubkey`). **Agent deletion is a real teardown** (per-Room cleanup best-effort, never gating Workspace removal — `agent.test.ts`).
- **Room creation is HUMAN-only at the funnel:** `createChannel` refuses a registered-agent author for top-level Rooms (corners allowed); `apps/gate/src/buzz.ts` mirrors it; `resolveRepositoryRoom` never creates; `setRoomRepository` refuses agent authors (`channel.test.ts`). Roles come from 39001/39002 projections, not kind:9000 ordering; `waitUntilMemberRole()` confirms; metadata readers take the newest 39000.
- Body drives `buzz-agent` over stdio (`acp.ts`). Live: `cd apps/body && npm run test:live`. Legacy `beeline serve <channel> <owner> <repo>`.

### Room turns, boundaries, read-only

- **The write boundary is only as strong as the harness's permission callback** (`session-sandbox.ts`, fail-closed: Room denies every mutation regardless of path; corner denies outside its worktree via `physicalPath`). codex-acp asks + real read-only mode; claude-agent-acp asks, no read-only mode; **pi-acp NEVER calls `requestPermission` — a pi Room's read-only rule is advisory** (`roomSandboxWarning`). Coverage: `session-sandbox.test.ts`, body.test.ts `Room sessions cannot write or execute at all`.
- **Read-only = ACP mode + MCP boundary:** Rooms mount `buzz-readonly-mcp`; edit sessions alone mount `buzz-dev-mcp`. `isReadOnlyInformationRequest` never projects ALLOW; `isChannelWorkIntent` opens directly; else the first mutating request needs a human-signed ALLOW; repo-less Rooms must name `owner/repo` (`repository-target.ts`). Room agent delegation uses visible signed kind:9 `buzz-agent-delegation` messages rooted in a verified human request; the target evaluates its own policy against that human, delegated corner work still requires approval, and host bounds/dedupe live in `agent-mention.ts`/`body.ts`. The separate human-authorized `buzz-agent-exchange` remains read-only; the retired hidden delegation transport must not return.
- **Reads and host-governed tools MUST flow:** `isReadOnlyMcpPermissionRequest` and `isBeelineAgentToolPermissionRequest` (`read-only-policy.ts`) run before Room's generic mutation denial; the latter admits only the MCP transport, while `authorize-or-request.ts` owns the action verdict/result. codex marks `_meta.is_mcp_tool_approval`; **claude-agent-acp marks nothing** (`mcp__<server>__<tool>`, `kind:'other'`). Never resolve a shell payload by name. Fixtures verbatim: `fixtures/claude-agent-acp-permissions.ts` (`scripts/capture-acp-permissions.mjs`; `room-read-only-permission.live.test.ts`).
- **Agent-initiated corners use the harness protocol** (`handleRoomPermissionRequest` → human card → open on ALLOW); `corner-request.ts` text marker is pi-only.
- **bwrap is HYGIENE under the permission handler, not a security floor** (`bwrap-sandbox.ts`): `--ro-bind / /`, `--tmpfs /tmp` (session paths under `/tmp` are ABSENT unless re-bound), writable harness state in both modes; corners add worktree + git common dir (unresolvable → fail OPEN) + `~/.no-mistakes`; detection fails open; `BUZZY_BODY_SANDBOX`. Network untouched; sockets pierce it. **Corners use a denylist sandbox** (writable root, no prompts: codex `agent-full-access`, claude `bypassPermissions`; canonical checkout/sibling corners/daemon workspace/credential stores protected). Coverage: `bwrap-sandbox.test.ts`, `harness-sandbox.live.test.ts`.
- **Workbench** (`workbench.ts`): bwrap-only quota tmpfs read via `/proc/<pid>/root`; `promptAgent` captures artifact bytes inside `runOnSession`, uploads after eviction; never durable or lazily resolved.
- **Newest Room directive is tail-framed at the single prompt door** (`promptAgent` repeats the request last). **Corner opening is idempotent by triggering event id** (`openSubchannelForRequest` joins direct-command, ACP-permission, and agent-tool paths); `read_corner`/`list_corners` are the recovery truth after an ambiguous tool outcome. A non-admin requester gets one audience-scoped approval card; requester/admin/owner may decide, and the indexed surface collapses the decision into that card. **Completion claims are host-grounded** (`replyInRoom` publishes positive or negative corner claims only against the same request's live `SubchannelInfo`; mission claims impossible). **An empty turn is not evidence** — never a domain claim. **A turn that moves into a corner still owes a visible reply** before `agent-turn:complete`. **Receipts precede activation**.
- **One transcript output per turn** (`AcpClient.sessionPrompt`: only the last run is durable `#t=agent-message`; growing runs feed replaceable `#t=agent-draft`, retracted at finish). Reasoning is live-only (`#t=agent-thought`). `stripAgentReplyPreamble` on draft and final (`acp.test.ts`).
- **`ROOM_AGENT_PROMPT_TIMEOUT_MS` is an idle window, not a turn cap.** Stall notice armed inside `runOnSession`, deferred on fresh inbound; 3 stalls bound blind retry; a stall notice never threads cross-channel. **A mid-turn message is QUEUED and acked** (`postSteerQueuedNotice`, `#t=steer-queued`) — never a stall; no shipped adapter supports steering; `inFlightSubchannelPolls` guards re-entrancy. **One mention → at most one corner** (`inFlightRequestIds`; `corner-open-guard.ts`). Addressing: `p=` mention or sole other participant.
- **Slash vocabularies** (`slash-command.ts`; unknown/chat-typed verbs get `t=slash-command-notice`, then deliver). **Target branch** (`target-branch.ts`: `/change-target-branch --branch <name>` from permission requests AND completed prose; checked BEFORE mutation escalation and `releaseRoomIntent`); Confirm → owner-only `setRoomTargetBranch`; `reconcileRoomTargetBranch` rebases every open corner. **Release flow** (`release-flow.ts`: git ground truth, held proposal, whole-message confirmation, ANNOTATED tag + `--follow-tags`; ask forced `informationOnly`).
- **Presence:** one replaceable `#t=agent-presence` per agent/Room; `isAgentPresenceOnline` uses `|now - observedAt| <= AGENT_PRESENCE_STALE_MS`; heartbeats retry, `created_at` stamped at PUBLISH time, coalesce only redundant repeats. **A planned restart is a non-event** — graceful stop publishes nothing; lease expiry is the death signal; `offline` only for a relay outage outliving the lease. Tiers `online|offline|dormant` + membership-backed `evicted`. Coverage: both `agent-presence.test.ts`.
- **Activity projection** (`activity.ts`): only terminal load-bearing tool calls reach `#t=agent-activity`; reads/MCP dropped but tallied (`rollup` + `observed`, MCP excluded) in the same pass; reads-only batches emit a summary, not silence. **Command palette** only from the published `buzz-agent-commands` record (`agent-commands-publish.ts`).
- **Model/effort:** `session/set_config_option` (axis + choice id; codex `{value,name}`, claude `{id,name}`). **Allow-list `{model, thought_level, effort, reasoning_effort}` only — `mode` never exposed** (`ALLOWED_AGENT_MODEL_CONFIG_CATEGORIES`, re-checked before every set). Records `buzz-agent-model-catalog` (self) + `buzz-agent-model-config` (human). Selection closed over the live catalog (`model-catalog.ts`); `model-unavailable` vs `validation-unavailable`; blocked startup keeps daemon alive, presence offline; `syncModelSelectionToRelay` validates per Room before first presence (`model-config.test.ts`).
- **Session control** (`session-scheduler.ts`): capacity mutex guards bookkeeping only, never I/O; per-Room budget under a dynamic ceiling (`BUZZY_BODY_MAX_SESSIONS*`); Rooms activate lazily; Grok warm 30min. **Corner continuity:** clean suspension keeps the harness conversation id (`session/load`); restart → one `session-reprime.ts` brief.
- **Chat files are link-only kind:9 references** (`attachment.ts`); relay rejects metadata-bearing images (422) — canonicalize (`canonicalizeImageForUpload`, mobile `avatar-png.ts`); previews via `preview.usebeeline.app`.

### Repository, corners, worktrees

- **The ROOM owns the repository** (`room-repository.ts`: admin-authored kind:30078 config, genesis fallback; `null` = chat-only). `repository-truth.ts` is the sole resolver: remote binding is truth; canonical checkout under `<supervisorRoot>/beeline/repositories/` cloned from ORIGIN, never the operator's tree. `readRoomRepositoryConfig` is tri-state — `unverified` is NOT absence. `git://github.com/<o>/<r>` is a credential-free identity marker; tokens via `/auth/github/room-token`; GitHub paths never consult ambient creds/helpers/`gh` (`repository-truth.test.ts`).
- **A corner worktree MUST be a clean top-level path** (`corner-isolation.ts`: `<parent>/.beeline-corners/<repo>/<id>` for paired checkouts; relay-origin keeps `.worktrees`; `assertCornerWorktreeIsolated`, `classifyCornerCommand` cd-guard). Never reintroduce a `.reader` worktree for Room reads. Provisioning is corner-local (`corner-toolchain.ts`: `npm ci` per corner, never shared `node_modules`). Coverage: `corner-isolation.test.ts`, `corner-toolchain.test.ts`.
- **Fast corner open uses Body-owned warm worktrees, not Treehouse lifecycle ownership** (`corner-warm-pool.ts`): detached, provisioned slots live in the repository corner pool, refresh to the current target and move/rebind through Git on take, then ordinary Body archive/reap destroys the assigned corner and background fill replaces the slot. The cold path remains the empty-pool fallback. Coverage: `corner-warm-pool.test.ts`.
- **Corner name/branch from the actual task** (`taskDescriptionFromCornerRequest`, `cornerObjectiveFromConversation`; write-once `task` tag on kind:9007); `modelCornerMetadata` hidden tool-free turn before creation; first turn seeded via `cornerOpenTaskPrompt` on both open paths (`corner-clarity.test.ts`).
- **Corner lifecycle authority is kind:30078 `buzz-corner-state:<id>`** (`corner-state.ts`, 90s lease); READY re-validated at `publishMergeReady`; `reconcileCornerExistence` proves parent + child. **Archived ONLY on landing or explicit close**; after land, an active turn drains without interruption, an idle warm session suspends immediately, then the worktree is reaped. Archive scope child-only (kind:9007 parent link, gate mirrors). `#t=buzz-agent-cancel` ≠ `#t=buzz-corner-close`; typed "close this corner" closes (`corner-close-intent.ts`); mobile `closed-corners.ts` tombstone. Coverage: `corner-state.test.ts`, `land-archive-deferral.test.ts`, `corner-close.live.test.ts`.
- **A close must reach an unserved corner:** `AbandonedCorner` + `pollAbandonedCornerCloses` (publishes-before-disk); `pollUntrackedCornerCloses` re-adopts corners outside `listSubchannels`' newest-N window. A corner belongs to the key that signed its kind:9007 (`getChannelCreator`); foreign corners skipped. A relay 4xx (except 408/429) is a verdict (`isNonRetryableRelayError` parks, stated once). `removeWorktree` never deletes the branch.
- **The worktree sweep may not delete what it has not inspected** (`corner-worktree-sweep.ts` keeps live/dirty/ahead/tracked/unreadable, repairs, asks the relay last; a failed `registeredWorktrees` aborts). Land polls run BEFORE the member poll; `landOnDirectRemote` decides only on a successful `ls-remote` (`corner-integrity.test.ts`). **Local-only repos land through the DAEMON** (`landInLocalCheckout`, fast-forward only); approvals are daemon state, never chat.

### Merge readiness, approval, landing

- **Approval is blanket for one corner's one merge (`h` + repo + target)**; tip/`reviewPatchId` record, never pin. `queryMergeApprovals`: live reader → configured → production authority; total failure keeps polling. Gate verifies signer + corner + repo + target branch while the protected push broker verifies that same signed approval; agents are refused regardless of role (`worker.ts`, `push-broker.ts`). Pickup is corner-WS push (`runApprovalLandingPass`), maintenance poll as recovery. **Every approval is CONSUMED AND ANSWERED** (`#t=buzz-merge-approval-ack`; mobile `approval-state.ts`). Coverage: `approval-acks.test.ts`, `push-broker.test.ts`.
- **A change review is one content-addressed artifact** (`publishChangeReviewArtifact` → one `change-review-artifact` pointer at `<corner>:<tip>:artifact`, size + SHA-256 verified; generation contract `change-review.ts`; mobile caches complete generations only). Coverage: `change-review.test.ts`, `buzz-rig-transport.test.ts`.
- **Branch-content sync is lazy and belongs to the corner model, never the daemon.** A committed feature tip gets a review card without a target-freshness check, and that card stays mounted. Only a signed merge press that cannot fast-forward starts one visible turn naming the exact target tip; the model owns sync/conflicts/test fixes/commit, then Body verifies and retries the signed ff-only land once. Original replies survive the gate; blockers stop conclude-watch. Coverage: `body-merge-landing.test.ts`, `body-conclude-watch.test.ts`, `merge-button.live.test.ts`.
- **Lifecycle-critical publications ride outages out** (`publishCritical`, `publish-delivery.ts`; 5xx/408/429 transient, other 4xx fail fast). `archived` = close requested, `archiveCompleted` gates skip-forever; `mergeTarget` set only after confirmed publish; `MergeOutcome.terminal` explicit. Coverage: body.test.ts `graceful relay-failure confirmation`.
- **Target movement has no watcher and no landing queue.** Commit watch publishes only new feature tips and never revalidates an already-mounted card. Each signed press attempts an ff-only land, runs at most one model-owned sync turn after a moved-target refusal, then attempts one more ff-only land; physical ref compare-and-set provides serialization. A failed press parks its approval until a new signed press. Coverage: `github-delivery.test.ts`, `body-merge-landing.test.ts`.
- **A rewritten corner feature ref is republished from Git truth, not daemon memory.** `publishMergeReady` reads the current feature ref from the remote's push endpoint; when that observed sha is not an ancestor of the new tip, it uses an exact `--force-with-lease=<ref>:<observed-sha>`. Review-delivery failure lines are consecutive-content deduped and seeded from the restored transcript. Coverage: `github-delivery.test.ts`.
- **A landed corner posts one typed recap in the PARENT Room from the LAND** (`recapLandedCorner` from both land paths; `landedTip` recorded BEFORE any publish; `landSummaryPosted` after publish; `landRecapSettled` holds the archive). The digest carries objective, delivery, deliberate omissions, destination/link, and verified human approver; `room-indexer.ts` projects it as `RoomViewMessage.landSummary`, rendered by mobile's `LedgerLandDigest`. `land-destination.ts` adds URL/not-fetched lines only when proven. CI watched ONCE (`ci-watch.ts`; only success/failure publish). Preview URL only when real (`preview-url.ts`; never on `MergeTarget`). Coverage: `land-summary.test.ts`, `land-close-digest.live.test.ts`, `room-indexer.test.ts`.
- **Transcript failure text carries no raw git plumbing** (`summarizeGitFailure`, `git-failure.ts`; gate reasons stay raw). Body advances any target only after an exact-tip signed approval from a human admin (`github-origin.live.test.ts`, `startup-protection.live.test.ts`).

## Daemon self-update and CLI bundle

- Authority `self-update.ts`, `self-update-manifest.ts` (ONE manifest seam), `self-update-cli.ts`. **`<prefix>/lib/beeline` is the ACTIVE BUNDLE ANCHOR** (symlink into `lib/beeline-releases/<id>` or a legacy real dir); forwarders derive `BEELINE_LIB_DIR` as the clean anchor path (no `pwd -P`, no `..`); `forwarderScript()` stays textually identical to install.sh's heredocs; detect with `lstat`; install.sh swings the anchor with rename(2), never `mv`. Stale pending journal → auto-rollback (`BEELINE_UPDATE_DISABLE=1` opts out). Coverage: `self-update-layout.test.ts`.
- **Supervised convergence is a 10-minute contract** (`managed-update.ts`: journal desired release + absolute deadline, quiesce, exit; `Restart=always` resurrects; the daemon never spawns its replacement; drain timers stay referenced). Coverage: `managed-update.test.ts`, `systemd-acceptance.test.ts`.
- `beeline-bundle.yml` builds linux-x64 on the self-hosted prod runner and commits to `relay-stack/web/dl/` (`scripts/publish-beeline-dl.mjs`, `docs/cli-bundle-channel.md`; darwin `enabled: false`). `npm run bundle:beeline` self-verifies via `scripts/verify-beeline-install.mjs`.

## Production host deploy on merge

- `deploy-host.yml` → `scripts/deploy-relay-host.sh` on a self-hosted runner ON the host (`beeline-runner`, label `beeline-prod-host`; sudoers rules enumerated in the script header — never widen in script). Swaps `relay-stack/web/` in place (inode kept; rsync `-a -O --no-p --no-o --no-g`); verifies PUBLIC URL against the CHECKOUT; auto-rollback from `web-backups/`.
- Two stacks: `relay-stack/` = isolated gate stack; `relay-stack/prod/` = production (its `README.md`); nginx stays under the `./relay-front:/etc/beeline-front` directory bind. **One `materializer` process owns push delivery + repo-event ingestion** (`apps/push-gateway/src/index.ts`); the retired `beeline-events.service`/standalone gateway must not return. Coverage: `npm run test:deploy`.

## Mobile OTA release

- **A merge is never an OTA release:** `mobile-ota.yml` orchestrates, `ota-release.mjs` owns beta/canary/exact-group EAS operations, and `ota-delivery-index.mjs` owns the cumulative pending → built → published → physical-device-confirmed state machine. Canary runs on the operator-run `emulator-5554` (runner has no SDK/KVM; 600s deadline); ledger records `previousProductionGroupId`; bypass only via recorded `workflow_dispatch`. Environment failures exit 2 → `blocked` (`park()`). A `beta-apk` binary is the ONLY canary vehicle (`EXPO_UPDATES_CHANNEL` baked at build). Native changes need a rebuild + runtimeVersion bump. Smoke reply waits start at phase markers (`publish-smoke-replies.ts`). Coverage: `mobileOtaRelease.test.ts`.

## GitHub identity and repository authority

- Authority `apps/auth/README.md`; `/auth/capabilities` gates GitHub on all five `BEELINE_GITHUB_*`; `npm test -w @beeline/auth`. Auth owns PKCE + a hashed one-use binding ceremony and never grants relay/Room/role/merge authority.
- **A repo an admin does not OWN links via the owner's App install** (`owner_grant_needed` + `install_url`; durable `beeline_github_room_link_requests`; daemon parks transiently; mobile `OwnerGrantNeededCard`). **Repository identity is the immutable id; auth heals transfers** (`resolveGitHubRepositoryAccess`: grant → id-heal → alias → GitHub redirect). The binding's `githubInstallationId` pin is a hint, never a veto. Postgres parameter numbers must be contiguous.
- Org installs cannot be verified via `GET /user/installations` (trust the state-bound redirect; refuse only on definitive denial); `reconcileGitHubInstallations` enumerates with the App JWT. Error handler never emits a silent 500. Install/update returns share the OAuth callback (`completeGitHubInstallation` first). Native returns have one authority `github-auth-session.ts`. New-key onboarding never persists before `seen` AND `confirmed`. **Repo event ingestion is a materializer consumer** (`events-service.ts`, `hosted-events.ts`); `beeline events daemon` must not return.

## GitHub key succession

- **Roles resolve succession-aware at the roster funnel** (`communityMembers(ctx, id, {predecessors})` → `inheritRolesThroughSuccession`; `BuzzClient` fetches `/auth/oidc/predecessors` once). Ledger `beeline_key_successions` (`apps/auth/src/store.ts`; helpers `resolveCurrentPubkey`/`sameIdentity` only). Room-token authority resolves the binding author to its CURRENT key; `Body.resolveBindingOwnerKey` feeds `findHumanMergeApproval`'s fallback. Gate `trustedReviewer` does NOT follow succession (known). Binding resolution follows the chain only via `apps/auth`'s injected resolver — the daemon gets none.
- **Zero re-invite migration is client-side** (`migrateSuccessorMemberships`; DMs/corners skipped); the production relay honors member-adds only from a room ADMIN → `MembershipProjectionTimeoutError` = NOT MIGRATABLE (`unmigratable-rooms.ts`).

## Mobile client (Happy fork)

- `apps/mobile` is vendored Happy, isolated from root workspaces (`npm run mobile:install`); `@beeline/*` via Metro aliases to `dist/` (`metro.config.js`, existence-guarded). One identity `app.usebeeline.mobile`/`beeline://`, one channel `production` (`appBranding.test.ts`); no `googleServicesFile`. Release APK `npm run apk:release`; `npm run e2e`. **BuzzRigTransport is write/live-only**; reads go through `RoomViewClient`; MMKV holds only scoped DTO responses + outbox. RN-mocked tests read `.props.children`, never `.children`.
- **Corner status is one canonical model** (`buzz/corners.ts`; prefer `display-status`); ONE oracle `resolveCornerLifecycle` fed by `cornerSummaryFromEvents` (merge-ready speaks only while nothing newer has). **Rendered verdict is three words — working | needs-human | finished** (`resolveCornerState`, `packages/buzz-client/src/corner-lifecycle.ts`; asks first) plus `stalled` for a provably offline agent. Corner agent identity via `resolveCornerCardAgentPubkey`/`resolveCornerViewAgentPubkey`. Attention card never renders inside the corner (`corner-oracle-surfaces.test.ts`).
- **Pinned corner line and thinking indicator are independent** (`selectPinnedCorner`, `room-indicators.ts`: any non-terminal corner is a candidate, gold/pulse for `live` only); `TurnProgressLine` from `latestAgentTurns` alone. Corner status is NEVER in the transcript (`CornerLiveBar.tsx` above the composer; `HullLivePulse`, never a sweep). **Leaving a corner is a lookup** (`corner-navigation.ts` `chatBackAction`; pushes via `cornerHref`). Header title via `channelHeaderTitle` (`null` → skeleton).
- **The foreground never blocks on the network** (`room-entry.ts` fan-out — new reads are independent `step()`s, never chain links); `no-foreground-blocking.test.ts` whitelists awaits and bans `cacheLiveSessionEvent` (singular). Live events coalesce per frame; projections identity-stable (`projection-identity.test.ts`); `renderItem` deps take `onlineVerdicts`/`useStable`, never raw presence. **Transcript FlatList is `inverted`** (`maintainVisibleContentPosition { minIndexForVisible: 1, autoscrollToTopThreshold: 50 }`, no manual scroll-follow; `marginBottom` is the gap ABOVE). Ledger leaf components stay `React.memo`'d.
- **Room-list deck is server-indexed** (#566/#567/#577/#598 superseded the old client-side `room-list-row.ts` logic, which now only supplies title formatting). `channels.tsx` renders `ChatListView.chats` from `RoomViewClient.chats()`; row circle = `ChatListItem.agentState?: 'needs-you'|'working'` (absent=idle), a max-severity rollup in `CHAT_LIST_SQL`'s `readChats()` (`room-indexer.ts`) over each corner's latest state + the Room's latest `agent-turn`. `room-deck-state.ts`'s `roomDeckState()` alone combines it with unread — never an inline ternary. Deck still: TWO piles NEEDS YOU / DOESN'T NEED YOU (no FINISHED/DIRECT); cells render mark/name/fact/count only; unread is room-level (`room-read-state.ts`, marked on return only). **Exactly ONE home surface** (`channels.tsx`; Personal Workspace differs only in `allowPeopleInvites`). Settings gear → `/buzz/settings` hub. Previews sanitized where stored (`roomPreviewText`, no SDK import). Coverage: `room-indexer.test.ts`, `room-deck-state.test.ts`, `channels.design.test.ts`.
- **A removed Room needs two local layers** (`removed-rooms.ts` tombstone + `removeChannel`); archive (kind:9002) ≠ Delete (kind:9008 `deleteRoom`); tombstones only for self-leave/legacy. **A visible control must act or explain itself** — no `if (!transport) return`; Members paints from cache (viewer added back — the roster cache omits the viewer), empty presence is UNKNOWN, handlers await `requireConnection()`. **Direct Room membership is the complete roster** (`room-participants.ts`, never intersected with Workspace rosters). Coverage: `agents.test.ts`.
- **Agent names** (`agent-display.ts`: soul → registered `displayName` → `fallbackAgentName`; rosters merged field-wise, soul never dropped, empty roster never committed; `resolvePendingAgentDisplay` null until hydrated). Two agent messages never share a bubble (`upsertChatMessages`, draft→final only); `isNew` never persisted, reveal consume-once (`message-reveal.ts`). Coverage: `agent-display.test.ts`, `local-cache.test.ts`.
- NIP-05 (`nip05.ts`, never throws); managed `@usebeeline.app` identity owned by `apps/auth` (`/.well-known/nostr.json`, `/nip05/claim`; both nginx configs proxy; GitHub ownership wins). `RepoPicker.tsx` is self-bounding (never wrap in a scroll container). Corner objective via `cornerObjectiveLine`/`CornerPlanPin`, never free-text (PR #165's wire must not return). Souls: `setAgentSoul` (human-signed, no authority); a soul author is not the agent (`agent.ts` rejects keys with a human kind:0). Workspace exit `leaveCommunity` (sole owner refused client-side).

## Android push notifications

- Registration `buzz-push-registration.ts`; `https://usebeeline.app/push` → `materializer:8788`. **Attention-only defaults** (explicit `p` mentions, DMs, waiting-on-human transitions). **The toggle reflects REGISTRATION truth** (`buzz-push-status.ts`; token fingerprints only in logs). Foreground policy `foreground-policy.ts` (suppress when active/open Room; never unconditional `shouldShowAlert: true`). At-most-once delivery `delivery-state.ts` (attention keys cleared by idle/concluded/closed, not `working`). `database.ts` reads event rows with active `channel_members`; no global subscription/replica feed. Ops `apps/push-gateway/README.md`.

## Community model

- Authority `community.ts` (`community.live.test.ts`): NIP-29 group, self-referencing `community=<h>` on kind:9007, 9000 roles, 39001/39002 projections. Workspace membership = direct membership in every live top-level Room for people; agents linked at Workspace scope, per-Room attach explicit. Invites are signed kind:30078 (`t=buzz-community-invite`, `d=SHA-256(token)`, `findCommunityInvite`). DMs (`direct-message.ts`): deterministic private `t=buzz-dm` Room; agent DMs strictly conversational — never borrow a repo or open a corner.

## Explicit external MCP capabilities (Trusty Squire)

- `external-mcp-capabilities.ts`: records store profile names only (`squire-credential-use` / `squire-app-access`, disjoint, `--access creator`, codex/claude only). Pairing runs `npx @trusty-squire/mcp connect`; daemon mounts use `server`. Squire is the one governed hole in the read-only boundary: inventory read-only; `use_credential`/`grant_app_access`/`revoke_app_access` need P1's Tier-2 `operation.execute` envelope (owner signature, one use, receipts; `grant_app_access` needs an hourly cap); host broker reverifies; Squire on a Body-owned file-only XDG store masked from agents (`squire-host-broker.test.ts`).

## Grok Mono Hull UI theme

- **Single source of truth: `/DESIGN.md`.** Tokens `buzz/groknight.ts` (grayscale, one accent `#d7af5f`, `radius: 3`; `diffAdded`/`diffRemoved` are the one chroma exception). A box wraps only something the user must act on or a non-repeating region; repeating units get whitespace/`hairlineDivider`. Primitives `MonoHull.tsx`; marks `brand.json`/`beeline-mark.json`.
- **Rooms and Corners render ONE ledger primitive** (`Ledger.tsx`, one `renderItem` branch). ONE message size; a user message is NEVER bolded/enlarged/inset — ownership reads from the byline; corners attribute like Rooms via `ledgerSpeakerKey` (`ledger-attribution.ts`, per run). Pinned by `transcriptSurfaces.test.ts`. Fonts IBM Plex Sans/Mono + Bricolage Grotesque (no `SpaceMono`); mono allowlist `Typography.test.ts`.
- Streaming lanes settle to one durable `agent-message`; only classified failure/merge/consequential action survives as one room-update line (`activity-timeline.ts`); no reply echo under agent turns; `HullMechanismReveal` pops only live-mounted rows (`MechanismMotion.test.ts`).
- **Identity marks are ONE component** (`IdentityMark.tsx`/`identity-mark.ts`: shape=type, curated `HUE_WHEEL`, fill, cypher, gold ring = live agent only). `IDENTITY_FILL_STATES` order stable; legacy `components/Avatar.tsx` family is a named exemption. A status is inscribed, never framed; persistent chrome carries no `HullSurface`; one glyph per concept (`vocabulary.ts`); wordmark `beeline.` with brass period. Android: a `flex:1` Text wrapping only Text inside a row View goes blank (`MonoMarkdown.test.ts`). Diff view: vertical-outer/horizontal-inner ScrollViews, plain Text rows.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise. Stay under 40KB — it is loaded into every agent request.
