# Room-list Messages section

Captured on the existing Android API 36 emulator (`emulator-5556`) at 1080 × 2400. The
debuggable Beeline build loaded this branch from Metro and retained its on-device fixture with
one `#welcome` Room and one `@system` direct message. The screenshot demonstrates that the Room
stays first and the DM renders below the restored `MESSAGES` section header.
