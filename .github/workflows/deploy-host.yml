name: Server release leg

on:
  workflow_call:
    inputs:
      phase:
        description: Build exact server images or promote those already-built images
        type: string
        required: true
      release_sha:
        type: string
        required: true
      release_version:
        type: string
        required: true

permissions:
  actions: read
  contents: read

concurrency:
  # GitHub keeps one running and one pending run in a concurrency group; a
  # newer pending merge replaces the previous pending merge.  Do not cancel an
  # active host deploy mid-cutover: it is safe to finish that one, then deploy
  # only the newest queued commit rather than churn every intermediate merge.
  group: deploy-relay-host
  cancel-in-progress: false

jobs:
  build:
    if: inputs.phase == 'build'
    runs-on: [self-hosted, beeline-prod-host]
    timeout-minutes: 15
    steps:
      - name: Find a downloadable server artifact for this exact release SHA
        id: reuse
        uses: actions/github-script@v7
        with:
          script: |
            const name = `server-artifact-${{ inputs.release_sha }}`;
            const artifacts = await github.paginate(github.rest.actions.listArtifactsForRepo, {
              owner: context.repo.owner, repo: context.repo.repo, name, per_page: 100,
            });
            const artifact = artifacts
              .filter((candidate) => candidate.name === name && !candidate.expired && candidate.workflow_run?.id)
              .sort((a, b) => b.created_at.localeCompare(a.created_at))[0];
            if (artifact) {
              try {
                await github.rest.actions.downloadArtifact({
                  owner: context.repo.owner, repo: context.repo.repo,
                  artifact_id: artifact.id, archive_format: 'zip',
                });
                core.setOutput('run_id', String(artifact.workflow_run.id));
              } catch (error) {
                core.info(`Artifact ${artifact.id} is no longer downloadable: ${error.message}`);
              }
            }
      - name: Reuse the exact server artifact
        if: steps.reuse.outputs.run_id != ''
        uses: actions/download-artifact@v4
        with:
          name: server-artifact-${{ inputs.release_sha }}
          path: ${{ runner.temp }}/server-release
          run-id: ${{ steps.reuse.outputs.run_id }}
          github-token: ${{ github.token }}
      - uses: actions/checkout@v4
        if: steps.reuse.outputs.run_id == ''
        with:
          ref: ${{ inputs.release_sha }}
      - name: Build SHA-pinned server images without changing production
        if: steps.reuse.outputs.run_id == ''
        run: ./scripts/build-release-server-images.sh
        env:
          BEELINE_RELEASE_SHA: ${{ inputs.release_sha }}
          BEELINE_RELEASE_VERSION: ${{ inputs.release_version }}
      - name: Record the verified server artifact identity
        if: steps.reuse.outputs.run_id == ''
        env:
          RELEASE_SHA: ${{ inputs.release_sha }}
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: |
          mkdir -p "$RUNNER_TEMP/server-release"
          node - "$RUNNER_TEMP/server-release/identity.json" "$RELEASE_VERSION" "$RELEASE_SHA" <<'NODE'
          const fs = require('node:fs');
          fs.writeFileSync(process.argv[2], JSON.stringify({
            version: process.argv[3], sourceSha: process.argv[4], images: ['auth', 'materializer'],
          }));
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: server-artifact-${{ inputs.release_sha }}
          path: ${{ runner.temp }}/server-release/identity.json
          if-no-files-found: error
          retention-days: 7

  promote:
    if: inputs.phase == 'promote'
    runs-on: [self-hosted, beeline-prod-host]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_sha }}
      - name: Promote exact prebuilt server images and verify public health
        run: ./scripts/deploy-relay-host.sh
        env:
          BEELINE_PREBUILT_IMAGES: '1'
          BEELINE_RELEASE_SHA: ${{ inputs.release_sha }}
          BEELINE_RELEASE_VERSION: ${{ inputs.release_version }}
